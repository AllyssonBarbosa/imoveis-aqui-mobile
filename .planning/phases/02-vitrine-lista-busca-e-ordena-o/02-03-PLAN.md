---
phase: 02-vitrine-lista-busca-e-ordena-o
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - lib/data/mocks/imoveis_fixture.dart
  - lib/data/datasources/parametros_consulta_imoveis.dart
  - lib/data/datasources/imovel_mock_datasource.dart
  - lib/presentation/vitrine/vitrine_cubit.dart
  - lib/presentation/vitrine/vitrine_screen.dart
  - test/data/imovel_mock_datasource_test.dart
  - test/presentation/vitrine_cubit_test.dart
  - test/presentation/vitrine_screen_test.dart
  - test/presentation/vitrine_fluxo_test.dart
autonomous: true
requirements: [VIT-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A vitrine rola infinita: ao chegar a ~90% do fim da lista, a próxima página é pedida seguindo só o 'next' do envelope cursor; quando next == null aparece 'Você chegou ao fim da lista' (VIT-05, D-13)."
    - "Rolar até o fim de Campinas mostra exatamente os 40 imóveis da fixture, cada id uma única vez e na ordem do servidor — sem cards duplicados ou embaralhados (critério 4 da fase)."
    - "Os quatro estados de lista existem e são distintos: loading (spinner), vazio da cidade (Indaiatuba), erro com 'Tentar de novo' e fim-da-lista; mais os dois estados de rodapé carregando-mais e erro-ao-carregar-mais (VIT-05)."
    - "Pedir 'carregar mais' duas vezes com uma página já em voo dispara uma única requisição; o mesmo cursor nunca é pedido duas vezes (edge VIT-05 idempotência)."
    - "Uma resposta de 'carregar mais' que chega depois de a consulta ter sido reiniciada (troca de cidade/busca/ordenação) é descartada pelo token de versão, e nada é emitido após close() (edge VIT-05 concorrência, D-13)."
    - "Falha ao carregar mais NÃO apaga os cards visíveis: a lista fica, o rodapé mostra 'Não foi possível carregar mais imóveis' + 'Tentar de novo', e só o toque nesse botão tenta de novo (rolar não re-dispara em loop)."
    - "Se a primeira página não preencher a tela e houver próxima página, a vitrine pede a próxima automaticamente após o primeiro frame (não trava a paginação em telas altas)."
    - "A DataSource mock simula o servidor (D-14): ~40 imóveis por cidade atendida (Campinas, Valinhos, Vinhedo) na forma exata do contrato, Indaiatuba sem imóveis (D-15), páginas de 10 via cursor opaco, 'next' como URL absoluta em /api/publico/imoveis/ com cidade/ordenacao/busca, latência de 500 ms — tudo determinístico."
    - "A DataSource mock é sem estado entre chamadas: consultas concorrentes para cidades diferentes devolvem páginas independentes e corretas, e seguir o mesmo 'next' duas vezes devolve a mesma página (edge API-04 concorrência)."
  artifacts:
    - path: "lib/data/mocks/imoveis_fixture.dart"
      provides: "gerador determinístico do acervo (40/cidade atendida + linhas do contrato)"
      contains: "linhasAcervoFixture"
    - path: "lib/data/datasources/parametros_consulta_imoveis.dart"
      provides: "mapeamento ConsultaImoveis -> query params do contrato (reuso na Fase 4)"
      contains: "parametrosDaConsulta"
    - path: "lib/data/datasources/imovel_mock_datasource.dart"
      provides: "paginação cursor simulada (buscar/seguir)"
      contains: "cursor"
    - path: "lib/presentation/vitrine/vitrine_cubit.dart"
      provides: "carregarMais com guarda e token; tentarNovamente para rodapé"
      contains: "carregarMais"
    - path: "lib/presentation/vitrine/vitrine_screen.dart"
      provides: "ScrollController + rodapés de paginação"
      contains: "Você chegou ao fim da lista"
    - path: "test/presentation/vitrine_screen_test.dart"
      provides: "renderização de todos os estados da vitrine"
      contains: "Tentar de novo"
  key_links:
    - from: "lib/presentation/vitrine/vitrine_screen.dart"
      to: "lib/presentation/vitrine/vitrine_cubit.dart"
      via: "listener do ScrollController chama carregarMais() a 90% do maxScrollExtent"
      pattern: "carregarMais"
    - from: "lib/presentation/vitrine/vitrine_cubit.dart"
      to: "lib/domain/usecases/buscar_imoveis_usecase.dart"
      via: "proximaPagina(conteudo.proximaPagina) com o 'next' opaco"
      pattern: "proximaPagina"
    - from: "lib/data/datasources/imovel_mock_datasource.dart"
      to: "lib/data/datasources/parametros_consulta_imoveis.dart"
      via: "o 'next' é montado e lido com os mesmos nomes de param do contrato"
      pattern: "parametrosDaConsulta"
  prohibitions:
    - "Uma falha ao carregar a próxima página nunca descarta os cards já visíveis."
    - "O Cubit e a tela nunca interpretam, montam ou alteram a URL/cursor de próxima página — só devolvem o 'next' opaco recebido (contrato §4, RESEARCH Pattern 5)."
---

<objective>
Turn the tracer's single page into the full infinite-scroll vitrine (VIT-05): the mock becomes a
realistic simulated server (≈40 imóveis per served city, cursor pagination with an absolute
`next`, 500 ms latency — D-14/D-15), the Cubit gains a guarded, version-tokened `carregarMais`
(D-13), and the screen gains the ScrollController trigger plus the loading-more / error-retry /
end-of-list footers.

Purpose: success criterion 4 of the phase — four list states and zero duplicated or shuffled
cards — on the same data layer Phase 4 will swap for the real API.
Output: expanded fixture + mock pagination + parameter mapper, Cubit pagination, screen
footers, unit/bloc/widget/e2e tests.
</objective>

<user_story>
**As a** visitante navegando a vitrine, **I want to** rolar a lista e ver mais imóveis chegando
sozinhos até o fim, com aviso de carregamento, de erro com "tentar de novo" e de fim da lista,
**so that** eu percorro todo o acervo da cidade sem ver cards repetidos ou fora de ordem.
</user_story>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-CONTEXT.md
@.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-RESEARCH.md
@.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-PATTERNS.md
@.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-01-SUMMARY.md
@.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md
@.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/imoveis.example.json
</context>

## Artifacts this phase produces (share delivered by this plan)

Flutter:
- `lib/data/mocks/imoveis_fixture.dart` (modified) — `linhasAcervoFixture()` now deterministic ≈40 rows per Campinas/Valinhos/Vinhedo (keeps contract rows 42, 57, 63 verbatim), Indaiatuba 0; constant `urlBaseFotosMock`; per-city bairro lists.
- `lib/data/datasources/parametros_consulta_imoveis.dart` (new) — `const String caminhoImoveisPublico = '/api/publico/imoveis/'`, `Map<String, String> parametrosDaConsulta(ConsultaImoveis consulta)`, `Cidade cidadeDoParametro(String valor)`.
- `lib/data/datasources/imovel_mock_datasource.dart` (modified) — cursor pagination: `urlBaseMock`, `tamanhoPagina` (default 10; `paraTeste(..., int tamanhoPagina = 10)`), real `seguir(String proximaPagina)`, `previous` populated.
- `lib/presentation/vitrine/vitrine_cubit.dart` (modified) — `Future<void> carregarMais()`, `tentarNovamente()` extended to the load-more error, private `_carregarProximaPagina()`.
- `lib/presentation/vitrine/vitrine_screen.dart` (modified) — now a `StatefulWidget` owning `ScrollController(keepScrollOffset: false)`; private footer widget for carregando-mais / erro-ao-carregar-mais / fim-da-lista.
- Tests: `test/presentation/vitrine_screen_test.dart` (new); `test/data/imovel_mock_datasource_test.dart`, `test/presentation/vitrine_cubit_test.dart`, `test/presentation/vitrine_fluxo_test.dart` (extended).

Django: none in this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Servidor simulado — acervo de ~40 imóveis por cidade e paginação cursor seguindo o 'next' (D-14, D-15, API-04)</name>
  <files>lib/data/mocks/imoveis_fixture.dart, lib/data/datasources/parametros_consulta_imoveis.dart, lib/data/datasources/imovel_mock_datasource.dart, test/data/imovel_mock_datasource_test.dart</files>
  <read_first>
    - lib/data/mocks/imoveis_fixture.dart, lib/data/datasources/imovel_mock_datasource.dart, lib/data/datasources/imovel_datasource.dart, lib/data/models/imovel_model.dart, lib/domain/entities/consulta_imoveis.dart, lib/domain/entities/ordenacao_vitrine.dart (from plan 02-01)
    - test/data/imovel_mock_datasource_test.dart (from plan 02-01 — extend, do not rewrite passing cases)
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/imoveis.example.json (row shape and the `next` URL form `.../api/publico/imoveis/?cursor=...&cidade=Campinas-SP`)
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md (§3, §4, §5, §7.2)
    - .planning/phases/02-vitrine-lista-busca-e-ordena-o/02-CONTEXT.md (D-14, D-15; discretion: page size, `cidade` param format)
  </read_first>
  <behavior>
    - linhasAcervoFixture() is deterministic (two calls are deeply equal), contains rows 42, 57, 63 exactly as in contrato/imoveis.example.json (e.g. id 42 titulo "Apartamento 2 quartos no Cambuí", preco_venda "450000.00"), has exactly 40 rows for each of Campinas, Valinhos, Vinhedo and none for Indaiatuba, and every row parses with ImovelModel.fromJson.
    - The fixture covers: all three finalidades, all four naturezas, at least one null foto_capa, at least one null area, at least one preco_venda with non-zero cents, at least one accented bairro ("Cambuí").
    - Walking Campinas with tamanhoPagina 10 (buscar, then seguir(next) until null) yields pages of 10,10,10,10, 40 unique ids, all Campinas, in criado_em-descending order across pages; the last page has next == null.
    - The first `next` is an absolute URL whose path is /api/publico/imoveis/ and whose query has cidade=Campinas-SP, ordenacao=mais_recentes and a cursor param; busca appears only when the consulta has a non-empty busca.
    - Future.wait of buscar(Campinas) and buscar(Valinhos) returns two correct, independent pages; seguir(sameNext) twice returns equal envelopes.
    - seguir with an undecodable cursor or an unknown ordenacao value throws FormatException.
  </behavior>
  <action>
    Expand `linhasAcervoFixture()` into a deterministic generator (no random-number generator and no wall-clock/current-time read — D-15; keep doc comments free of those API names too, because the negative grep in <verify> also matches comments) that keeps the 3 contract example rows verbatim and generates the rest so Campinas (id 1), Valinhos (id 2) and Vinhedo (id 3) have exactly 40 rows each, Indaiatuba (id 4) none. Generated row for city index c (0..2) and local index k: `id` = 100*(c+1)+k (skip collisions with 42/57/63); `natureza` cycles APARTAMENTO, CASA, TERRENO, LOTE by k%4; `finalidade` cycles VENDA, ALUGUEL, VENDA_E_ALUGUEL by k%3; `preco_venda` null for ALUGUEL else a 2-decimal string growing with k (every row with k%7 == 3 gets 50 cents); `preco_aluguel` null for VENDA else a 2-decimal string; `quartos` 0 for TERRENO/LOTE else 1 + k%4, `suites` ≤ quartos, `vagas` k%3; `area` null when k%10 == 9 else a 2-decimal string; `foto_capa` null when k%5 == 4 else `'$urlBaseFotosMock/imovel-$id/800/450'` with `urlBaseFotosMock = 'https://picsum.photos/seed'` (a single constant so it is swappable in one line — dev-only fixture, removed in Phase 4); `bairro` cycles a per-city list (Campinas: Cambuí, Taquaral, Barão Geraldo, Centro, Guanabara, Nova Campinas, Castelo, Jardim Proença; Valinhos: Centro, Jardim das Palmeiras, Vila Santana, Chácaras Alpina, Jardim Pinheiros; Vinhedo: Centro, Santa Rosa, Jardim Nova Vinhedo, Capela, Vista Alegre); `titulo` "<Natureza> <n> quartos no <bairro>" (or "<Natureza> no <bairro>" when quartos is 0); fixed Portuguese `descricao`; `caracteristicas` a k-dependent slice of a fixed list; `criado_em` = a fixed UTC base instant minus (c*40+k)*5 hours, ISO 8601; nested `cidade` map from `cidadesDoFixture`.

    Create `parametros_consulta_imoveis.dart` (contract §5 names, D-04 snake_case): `caminhoImoveisPublico`, `parametrosDaConsulta(ConsultaImoveis)` returning `cidade` as `"<nome>-<uf>"` (provisional contract §7.2 natural key, isolated here — D-15 of F1 and CONTEXT discretion), `ordenacao` as `valorApi`, and `busca` only when non-empty after trim; `cidadeDoParametro(String)` splits at the LAST hyphen (city names may contain hyphens). Phase 4's remote DataSource reuses this file.

    In `ImovelMockDataSource`: add `tamanhoPagina` (default 10; `paraTeste` gains `int tamanhoPagina = 10`) and `urlBaseMock = 'https://mock.imoveisaqui.local' + caminhoImoveisPublico`. Both `buscar` and `seguir` go through one private pipeline (filter by cidade -> [busca/ordenacao hooks, still pass-through until plan 02-05] -> order by criado_em desc then id desc -> slice [offset, offset+tamanhoPagina)). The opaque cursor is `base64Url` of `"o=<offset>"` (mimics DRF's opaque token). `next` = `urlBaseMock` with `parametrosDaConsulta(...)` plus `cursor` for the next offset, or null at the end; `previous` likewise for the previous offset (null on the first page). `seguir(proximaPagina)` parses the URL's query parameters back (cidade via `cidadeDoParametro`, ordenacao by `valorApi`, busca, cursor), throwing `FormatException` for an undecodable cursor or unknown ordenacao — simulating the server's 400, which the repository already turns into `Result.failure`. The mock keeps no mutable per-query state between calls (everything is derived from the arguments), and the 500 ms latency applies to both methods (skipped entirely when zero).

    Extend `test/data/imovel_mock_datasource_test.dart` with every <behavior> case (use `paraTeste(linhas: linhasAcervoFixture(), tamanhoPagina: 10)`).
  </action>
  <verify>
    <automated>flutter test test/data/imovel_mock_datasource_test.dart</automated>
    <fails_when>non-zero exit, or "Some tests failed" in the output, or "No tests ran"</fails_when>
    <automated>! grep -nE "Random|DateTime\.now" lib/data/mocks/imoveis_fixture.dart lib/data/datasources/imovel_mock_datasource.dart</automated>
    <fails_when>any matching line is printed (non-deterministic mock, violates D-15)</fails_when>
    <automated>flutter analyze</automated>
    <fails_when>non-zero exit, or "issues found" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - The mock test walks Campinas to the end and asserts 40 unique ids in 4 pages with the last next null; asserts Indaiatuba empty; asserts the `next` URL shape (path /api/publico/imoveis/, cidade=Campinas-SP, ordenacao=mais_recentes, cursor present).
    - The fixture test asserts determinism, rows 42/57/63 verbatim, 40 per served city, and coverage of null foto_capa, null area, cents, all finalidades and naturezas.
    - The concurrency test (Future.wait on two cities) and the idempotent seguir test pass.
    - `lib/data/datasources/parametros_consulta_imoveis.dart` contains `parametrosDaConsulta`, `cidadeDoParametro` and `/api/publico/imoveis/`.
  </acceptance_criteria>
  <done>The mock behaves like the paginated Django endpoint (40 per served city, opaque cursor, absolute next, deterministic), proven by data-layer tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VitrineCubit.carregarMais com guarda, token de versão e retry explícito (VIT-05, D-13)</name>
  <files>lib/presentation/vitrine/vitrine_cubit.dart, test/presentation/vitrine_cubit_test.dart</files>
  <read_first>
    - lib/presentation/vitrine/vitrine_cubit.dart, lib/presentation/vitrine/vitrine_state.dart, lib/domain/usecases/buscar_imoveis_usecase.dart (from plan 02-01)
    - test/presentation/vitrine_cubit_test.dart (from plan 02-01 — extend)
    - .planning/phases/02-vitrine-lista-busca-e-ordena-o/02-RESEARCH.md (Pattern 1, Pattern 2, Pattern 4, Pitfall 1, Pitfall 2)
  </read_first>
  <behavior>
    - From VitrineCarregada(10 itens, proximaPagina "p2"): carregarMais() emits carregandoMais true, calls proximaPagina("p2") once, then emits VitrineCarregada(20 itens in order old+new, proximaPagina from the new page, carregandoMais false).
    - Two carregarMais() calls while the first is in flight (Completer) -> proximaPagina called exactly once.
    - proximaPagina == null (end of list) -> carregarMais() makes no call and emits nothing.
    - Conteudo not VitrineCarregada (carregando, erro, vazio, semResultado) -> carregarMais() makes no call.
    - Failure -> itens unchanged, carregandoMais false, erroAoCarregarMais true; another carregarMais() (scroll) makes no call; tentarNovamente() clears the flag, calls proximaPagina with the same cursor and appends on success.
    - carregarMais in flight, then carregar(other city) -> the late page is discarded; the final state holds only the other city's first page.
    - close() while carregarMais is in flight -> no exception, no emit after close.
  </behavior>
  <action>
    Add `carregarMais()` to `VitrineCubit` following RESEARCH Pattern 4 + Pattern 2: read `state.conteudo`; return unless it is `VitrineCarregada` with `carregandoMais == false`, `erroAoCarregarMais == false` and `proximaPagina != null` (the error flag blocks automatic re-triggering from the scroll listener — retry is explicit only). Capture the current `_versaoConsulta` (do NOT increment it: a reset elsewhere increments it and invalidates this request), emit `carregandoMais: true` synchronously before the first await (Pitfall 2), call `_buscarImoveis.proximaPagina(proximaPagina)` with the opaque value untouched, then return early if the version changed or `isClosed`. Re-read `state.conteudo` after the await and, via an exhaustive switch on the Result: success -> append `data.itens` after the existing itens, set `proximaPagina: data.proximaPagina`, `carregandoMais: false`; failure -> `carregandoMais: false, erroAoCarregarMais: true` keeping itens (prohibition: never drop visible cards); loading -> ignore. Put the fetch in a private `_carregarProximaPagina()` so `tentarNovamente()` can reuse it: when conteudo is `VitrineCarregada` with `erroAoCarregarMais`, clear the flag and call it; when conteudo is `VitrineErro`, keep the existing `_reiniciar()` behavior.

    Extend `test/presentation/vitrine_cubit_test.dart` (bloc_test + mocktail; seed states with `seed:`; Completers for in-flight control) with every <behavior> case.
  </action>
  <verify>
    <automated>flutter test test/presentation/vitrine_cubit_test.dart</automated>
    <fails_when>non-zero exit, or "Some tests failed" in the output, or "No tests ran"</fails_when>
    <automated>flutter analyze</automated>
    <fails_when>non-zero exit, or "issues found" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - bloc tests prove: append in order, single request under double call, no call at end-of-list, no call outside VitrineCarregada, failure keeps itens + sets erroAoCarregarMais, scroll does not auto-retry after failure, tentarNovamente retries and appends, stale page after carregar(other city) discarded, close during load-more is safe.
    - `lib/presentation/vitrine/vitrine_cubit.dart` contains `carregarMais`, `erroAoCarregarMais` and `isClosed`.
  </acceptance_criteria>
  <done>The Cubit paginates exactly once per cursor, never loses visible cards on failure, and discards stale pages.</done>
</task>

<task type="auto">
  <name>Task 3: Scroll infinito na tela — gatilho a 90%, rodapés carregando/erro/fim e estados cheios (VIT-05)</name>
  <files>lib/presentation/vitrine/vitrine_screen.dart, test/presentation/vitrine_screen_test.dart, test/presentation/vitrine_fluxo_test.dart</files>
  <read_first>
    - lib/presentation/vitrine/vitrine_screen.dart, lib/presentation/vitrine/vitrine_cubit.dart (Task 2), lib/presentation/vitrine/widgets/imovel_card.dart
    - test/presentation/vitrine_fluxo_test.dart (from plan 02-01 — extend), test/presentation/cidade_selecao_screen_test.dart (MockCubit + whenListen pattern)
    - .planning/phases/02-vitrine-lista-busca-e-ordena-o/02-RESEARCH.md (Pattern 4 — 90% threshold)
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-UI-SPEC.md (Spacing, Typography)
  </read_first>
  <action>
    Convert `VitrineScreen` into a `StatefulWidget` that owns a `ScrollController(keepScrollOffset: false)` (so a reset list — new search/sort/city — always starts at the top, D-13) and disposes it. Its listener calls `context.read<VitrineCubit>().carregarMais()` when `position.pixels >= position.maxScrollExtent * 0.9` (RESEARCH Pattern 4; the Cubit guard absorbs repeated calls). After a frame in which the conteudo is `VitrineCarregada` with `proximaPagina != null`, `carregandoMais == false`, `erroAoCarregarMais == false` and the list does not scroll (`maxScrollExtent <= 0`), schedule one `carregarMais()` via a post-frame callback so tall screens do not stall.
    In the `VitrineCarregada` branch the `ListView.builder` (with the controller) gets `itemCount: itens.length + 1`; the last item is a private footer: `carregandoMais` -> small centered `CircularProgressIndicator` with 16px padding; `erroAoCarregarMais` -> "Não foi possível carregar mais imóveis" + `TextButton` "Tentar de novo" calling `tentarNovamente()`; `proximaPagina == null` -> "Você chegou ao fim da lista" (`bodyMedium`, `colorScheme.onSurfaceVariant`); otherwise an empty box. Keep the full-page bodies from plan 02-01 (carregando spinner, vazio copy with the city name, semResultado copy, erro + "Tentar de novo"), all via the exhaustive switch with no default branch.

    Create `test/presentation/vitrine_screen_test.dart` with a `MockCubit<VitrineState>` (whenListen) asserting: carregando shows a `CircularProgressIndicator` (use `pump()`, never `pumpAndSettle`); vazioNaCidade shows "Ainda não há imóveis anunciados em Campinas."; semResultado shows its own text, different from the empty-city text (D-09); erro shows "Tentar de novo" and tapping it calls `tentarNovamente()`; carregada with carregandoMais shows the footer spinner; with erroAoCarregarMais shows "Não foi possível carregar mais imóveis" and tapping "Tentar de novo" calls `tentarNovamente()`; with proximaPagina null shows "Você chegou ao fim da lista"; dragging a long carregada list to the bottom calls `carregarMais()`.
    Extend `test/presentation/vitrine_fluxo_test.dart` with an END-TO-END scroll test on the real stack (`ImovelMockDataSource.paraTeste(linhas: linhasAcervoFixture(), tamanhoPagina: 10)`, zero latency): for Campinas, drag the list upward repeatedly (pumping between drags) until "Você chegou ao fim da lista" is found, then read the `VitrineCubit` from the element tree and assert its conteudo is `VitrineCarregada` with 40 itens, 40 unique ids, all Campinas, in the same order as walking the mock directly.
  </action>
  <verify>
    <automated>flutter test test/presentation/vitrine_screen_test.dart test/presentation/vitrine_fluxo_test.dart</automated>
    <fails_when>non-zero exit, or "Some tests failed" in the output, or "No tests ran"</fails_when>
    <automated>flutter test</automated>
    <fails_when>non-zero exit, or "Some tests failed" in the output</fails_when>
    <automated>flutter analyze</automated>
    <fails_when>non-zero exit, or "issues found" in the output</fails_when>
    <human-check>
      <test>At end of phase (Django dev server running, app on an Android emulator), enter Campinas/SP and fling to the bottom several times; then switch to Indaiatuba/SP via the top selector.</test>
      <expected>New cards keep arriving with a small spinner at the bottom (≈0.5 s mock latency), no card repeats, the list ends with "Você chegou ao fim da lista"; Indaiatuba shows "Ainda não há imóveis anunciados em Indaiatuba.".</expected>
      <why_human>Real scroll physics, latency feel and the absence of visual duplicates during fast flings are best judged on a device.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `test/presentation/vitrine_screen_test.dart` covers carregando, vazioNaCidade, semResultado (text differs from vazio), erro + retry tap, footer carregandoMais, footer erroAoCarregarMais + retry tap, fim-da-lista, and drag-to-bottom -> carregarMais.
    - The e2e scroll test ends with 40 unique Campinas ids in server order in the Cubit state.
    - `lib/presentation/vitrine/vitrine_screen.dart` contains `keepScrollOffset: false`, `0.9`, "Você chegou ao fim da lista" and "Não foi possível carregar mais imóveis".
    - Full `flutter test` green and `flutter analyze` clean.
  </acceptance_criteria>
  <done>The vitrine scrolls infinitely with all list and footer states, proven end-to-end to show every Campinas imóvel exactly once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/ (mock today, API in Phase 4) -> Cubit | Paged results and the opaque `next` reference cross into presentation |
| app -> picsum.photos | Dev-only fixture photos are fetched from a third-party image host |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Denial of Service | VitrineScreen scroll listener / VitrineCubit.carregarMais | low | mitigate | Guard on carregandoMais / erroAoCarregarMais / proximaPagina; flag set before the first await; failed load-more never auto-retries on scroll (explicit "Tentar de novo" only) |
| T-02-03-02 | Tampering | `next` reference handling | low | mitigate | Presentation never parses or edits the `next` value; the data layer alone reads it; malformed cursor -> FormatException -> Result.failure -> footer error, never a crash |
| T-02-03-03 | Information Disclosure | urlBaseFotosMock (picsum.photos) | low | accept | Dev-only fixture removed with the mock in Phase 4; requests carry no user data (only the device IP, as with any image fetch); single constant makes it swappable in one line |
| T-02-03-04 | Denial of Service | VitrineCubit lifecycle | low | mitigate | Version token + isClosed checked after every await in carregarMais |
</threat_model>

<verification>
- `flutter test` (full suite) green; `flutter analyze` clean.
- No `Random` / `DateTime.now` in the mock or fixture.
- E2E scroll test: 40 unique Campinas ids in server order.
</verification>

<success_criteria>
- Infinite scroll with loading, empty, error/retry and end-of-list states, no duplicated or shuffled cards (VIT-05, D-13).
- The mock is a deterministic, stateless simulated server honoring the contract's cursor envelope (D-14, D-15, API-04).
</success_criteria>

<output>
Create `.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-03-SUMMARY.md` when done
</output>
