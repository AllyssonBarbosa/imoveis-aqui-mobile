---
phase: 02-vitrine-lista-busca-e-ordena-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - lib/domain/entities/imovel.dart
  - lib/domain/entities/ordenacao_vitrine.dart
  - lib/domain/entities/consulta_imoveis.dart
  - lib/domain/entities/pagina_imoveis.dart
  - lib/domain/repositories/imovel_repository.dart
  - lib/domain/usecases/buscar_imoveis_usecase.dart
  - lib/data/models/imovel_model.dart
  - lib/data/models/imovel_model.freezed.dart
  - lib/data/models/imovel_model.g.dart
  - lib/data/models/imoveis_envelope_model.dart
  - lib/data/models/imoveis_envelope_model.freezed.dart
  - lib/data/models/imoveis_envelope_model.g.dart
  - lib/data/datasources/imovel_datasource.dart
  - lib/data/datasources/imovel_mock_datasource.dart
  - lib/data/mocks/imoveis_fixture.dart
  - lib/data/repositories/imovel_repository_impl.dart
  - lib/presentation/vitrine/vitrine_state.dart
  - lib/presentation/vitrine/vitrine_state.freezed.dart
  - lib/presentation/vitrine/vitrine_cubit.dart
  - lib/presentation/vitrine/vitrine_screen.dart
  - lib/presentation/vitrine/apresentacao_imovel.dart
  - lib/presentation/vitrine/widgets/imovel_card.dart
  - lib/presentation/vitrine/widgets/foto_capa_imovel.dart
  - lib/presentation/cidade_selecao/cidade_selecao_screen.dart
  - lib/di/injection.config.dart
  - test/presentation/vitrine_fluxo_test.dart
  - test/presentation/vitrine_cubit_test.dart
  - test/data/imovel_repository_impl_test.dart
  - test/data/imovel_mock_datasource_test.dart
  - test/presentation/imovel_card_test.dart
  - test/presentation/apresentacao_imovel_test.dart
  - test/presentation/cidade_selecao_screen_test.dart
  - test/presentation/seletor_cidade_topo_test.dart
  - test/main_launch_routing_test.dart
autonomous: true
requirements: [VIT-01, VIT-02, API-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Ao entrar numa cidade atendida (estado autorizadaEAtendida), o visitante vê a vitrine dessa cidade: o seletor '{Cidade}, {UF}' no topo e, abaixo, a lista de imóveis vinda da DataSource mock pela cadeia UseCase -> Repository -> DataSource (VIT-01, D-14)."
    - "A vitrine mostra somente imóveis da cidade escolhida — o casamento é feito na camada data/ pela chave natural nome+uf (contrato §7.2, F1/D-15), nunca no widget/Cubit (VIT-01)."
    - "Uma cidade atendida sem imóveis na fixture (Indaiatuba/SP) mostra o estado vazio próprio 'Ainda não há imóveis anunciados em {Cidade}.' — determinístico, sem aleatoriedade (D-15)."
    - "Falha da DataSource na primeira página vira o estado de erro com 'Tentar de novo' (Result.failure -> conteúdo erro), e tocar em 'Tentar de novo' recarrega a vitrine (D-13, sem try/catch genérico na UI)."
    - "Trocar de cidade (ou sair da vitrine) com uma consulta em voo nunca lança erro nem mostra imóveis da cidade anterior: a resposta obsoleta é descartada pelo token de versão e nada é emitido depois de close() (D-13)."
    - "Cada card mostra, na mesma ordem em todos os cards, foto de capa larga no topo (16:9), título, preço, natureza, bairro e quartos, dentro de um Card do Material 3 (VIT-02, D-01)."
    - "Finalidade VENDA_E_ALUGUEL mostra os dois preços empilhados ('Venda R$ 450.000' / 'Aluguel R$ 2.500/mês'); VENDA mostra só o de venda; ALUGUEL só o de aluguel com '/mês' (D-02)."
    - "Preços em BRL pt_BR sem centavos quando zerados ('R$ 450.000') e com centavos quando existem ('R$ 450.000,50'); a conversão string->número->texto vive fora do widget, em apresentacao_imovel.dart (D-03)."
    - "foto_capa nula ou imagem que falha mostram o placeholder verde claro com ícone de casa, na mesma proporção 16:9 da foto — carregando, erro e carregada têm a mesma altura (D-04, RESEARCH Pitfall 6)."
    - "O repositório de imóveis depende só da interface ImovelDataSource; trocar mock por real na Fase 4 é mudar a anotação de DI de uma classe, sem tocar domain/ ou presentation/ (API-04, F1/D-14)."
  artifacts:
    - path: "lib/domain/repositories/imovel_repository.dart"
      provides: "interface ImovelRepository (buscarImoveis / buscarProximaPagina) retornando Result<PaginaImoveis>"
      contains: "abstract class ImovelRepository"
    - path: "lib/data/datasources/imovel_datasource.dart"
      provides: "fronteira trocável mock->real da camada data (API-04)"
      contains: "abstract class ImovelDataSource"
    - path: "lib/data/datasources/imovel_mock_datasource.dart"
      provides: "servidor simulado registrado como ImovelDataSource (D-14)"
      contains: "@LazySingleton(as: ImovelDataSource)"
    - path: "lib/presentation/vitrine/vitrine_state.dart"
      provides: "VitrineState (ordenacao, termoBusca, conteudo) + união selada ConteudoVitrine"
      contains: "sealed class ConteudoVitrine"
    - path: "lib/presentation/vitrine/vitrine_cubit.dart"
      provides: "VitrineCubit com carregar/tentarNovamente e token de versão"
      contains: "_versaoConsulta"
    - path: "lib/presentation/vitrine/widgets/imovel_card.dart"
      provides: "card do imóvel (D-01..D-04)"
      contains: "class ImovelCard"
    - path: "lib/presentation/vitrine/apresentacao_imovel.dart"
      provides: "formatação BRL e rótulos fora do widget (D-03)"
      contains: "formatarPrecoBrl"
  key_links:
    - from: "lib/presentation/cidade_selecao/cidade_selecao_screen.dart"
      to: "lib/presentation/vitrine/vitrine_screen.dart"
      via: "o estado AutorizadaEAtendida monta BlocProvider<VitrineCubit> + VitrineScreen no lugar do antigo placeholder"
      pattern: "VitrineScreen"
    - from: "lib/presentation/vitrine/vitrine_cubit.dart"
      to: "lib/domain/usecases/buscar_imoveis_usecase.dart"
      via: "o Cubit chama o use case e faz switch exaustivo sobre Result"
      pattern: "BuscarImoveisUseCase"
    - from: "lib/data/repositories/imovel_repository_impl.dart"
      to: "lib/data/datasources/imovel_datasource.dart"
      via: "injeção da interface (nunca da classe mock)"
      pattern: "ImovelDataSource"
    - from: "lib/presentation/vitrine/widgets/imovel_card.dart"
      to: "lib/presentation/vitrine/apresentacao_imovel.dart"
      via: "o card só exibe strings já formatadas pelo apresentador"
      pattern: "linhasDePreco"
  prohibitions:
    - "UI, Cubit e domain nunca recebem nem guardam o acervo inteiro de imóveis — filtrar por cidade/busca/ordenar/paginar acontece só dentro da DataSource trocável em data/ (D-14, regra 'nada é recalculado no aparelho')."
    - "Nenhum Widget faz chamada de rede nem decide regra de negócio; o card só renderiza valores já vindos do servidor, formatados pelo apresentador (constraint de Clean Architecture, D-03)."
    - "Nenhum try/catch genérico na camada de apresentação — toda chamada falível chega ao Cubit como Result selado e vira um estado explícito (constraint do projeto)."
---

<objective>
Lead the phase with a production-quality tracer: the visitor who enters a served city sees that
city's imóveis, loaded through every layer (mock DataSource -> Repository -> UseCase -> Cubit ->
screen), wired into the existing city-selection screen. Then expand the card to the full VIT-02
layout (photo, title, price, natureza, bairro, quartos).

Purpose: prove the vitrine architecture end-to-end on the first commit (API-04's swappable
DataSource boundary, the Cubit's version token for D-13, the sealed state union) before
pagination (02-03), cities-from-API (02-04) and search/sort (02-05) build on it. Implements
D-01, D-02, D-03, D-04, D-13 (reset/discard), D-14 (mock is a simulated server in data/), D-15
(deterministic empty city).
Output: Imóvel domain/data/presentation stack, VitrineScreen hosted by CidadeSelecaoScreen, full
ImovelCard, tests.
</objective>

<user_story>
**As a** visitante que acabou de entrar na minha cidade, **I want to** ver a vitrine com os imóveis
dessa cidade em cards consistentes (foto, título, preço, natureza, bairro, quartos), **so that** eu
navego o acervo real da cidade sem login — com o dado vindo pronto da camada de dados, nunca
recalculado no aparelho.
</user_story>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-CONTEXT.md
@.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-RESEARCH.md
@.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-PATTERNS.md
@.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md
@.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/imoveis.example.json
@.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-UI-SPEC.md
@./.claude/CLAUDE.md
</context>

## Artifacts this phase produces (share delivered by this plan)

Flutter (new files unless noted):
- `lib/domain/entities/imovel.dart` — `enum FinalidadeImovel { venda, aluguel, vendaEAluguel }`, `enum NaturezaImovel { casa, apartamento, terreno, lote }`, `class Imovel` (fields `id`, `titulo`, `finalidade`, `precoVenda`, `precoAluguel`, `bairro`, `cidade`, `fotoCapa`, `natureza`, `quartos`; manual `==`/`hashCode`/`toString`).
- `lib/domain/entities/ordenacao_vitrine.dart` — `enum OrdenacaoVitrine { maisRecentes, precoAsc, precoDesc, areaAsc, areaDesc }` with `final String valorApi` (`mais_recentes`, `preco_asc`, `preco_desc`, `area_asc`, `area_desc`).
- `lib/domain/entities/consulta_imoveis.dart` — `class ConsultaImoveis { Cidade cidade; String? busca; OrdenacaoVitrine ordenacao }`.
- `lib/domain/entities/pagina_imoveis.dart` — `class PaginaImoveis { List<Imovel> itens; String? proximaPagina; bool get ehUltima }`.
- `lib/domain/repositories/imovel_repository.dart` — `abstract class ImovelRepository { buscarImoveis(ConsultaImoveis); buscarProximaPagina(String) }`.
- `lib/domain/usecases/buscar_imoveis_usecase.dart` — `class BuscarImoveisUseCase { call(ConsultaImoveis); proximaPagina(String) }`.
- `lib/data/models/imovel_model.dart` — `ImovelModel` (freezed + json, contract §3.1+§3.2 shape) with `Imovel paraEntidade()`.
- `lib/data/models/imoveis_envelope_model.dart` — `ImoveisEnvelopeModel { next, previous, results }` with `PaginaImoveis paraPagina()`.
- `lib/data/datasources/imovel_datasource.dart` — `abstract class ImovelDataSource { buscar(ConsultaImoveis); seguir(String proximaPagina) }`.
- `lib/data/datasources/imovel_mock_datasource.dart` — `class ImovelMockDataSource implements ImovelDataSource` (default ctor + `@visibleForTesting ImovelMockDataSource.paraTeste(...)`).
- `lib/data/mocks/imoveis_fixture.dart` — `const List<Map<String, Object?>> cidadesDoFixture`, `List<Map<String, Object?>> linhasAcervoFixture()`.
- `lib/data/repositories/imovel_repository_impl.dart` — `class ImovelRepositoryImpl implements ImovelRepository`.
- `lib/presentation/vitrine/vitrine_state.dart` — `VitrineState` (freezed data class) + `sealed class ConteudoVitrine` with variants `VitrineCarregando`, `VitrineVaziaNaCidade`, `VitrineSemResultado`, `VitrineErro`, `VitrineCarregada`.
- `lib/presentation/vitrine/vitrine_cubit.dart` — `class VitrineCubit` (`carregar`, `tentarNovamente`, private `_versaoConsulta`, `_reiniciar`).
- `lib/presentation/vitrine/vitrine_screen.dart` — `class VitrineScreen`.
- `lib/presentation/vitrine/apresentacao_imovel.dart` — `formatarPrecoBrl`, `linhasDePreco`, `rotuloNatureza`, `rotuloQuartos`.
- `lib/presentation/vitrine/widgets/imovel_card.dart` — `class ImovelCard`.
- `lib/presentation/vitrine/widgets/foto_capa_imovel.dart` — `class FotoCapaImovel` (+ private placeholder widget).
- Modified: `lib/presentation/cidade_selecao/cidade_selecao_screen.dart` (new optional ctor param `criarVitrineCubit`, `AutorizadaEAtendida` renders the vitrine), `pubspec.yaml` (+`intl`, +`cached_network_image`, dev +`fake_async`), `lib/di/injection.config.dart` (regenerated).
- Tests: `test/presentation/vitrine_fluxo_test.dart`, `test/presentation/vitrine_cubit_test.dart`, `test/data/imovel_repository_impl_test.dart`, `test/data/imovel_mock_datasource_test.dart`, `test/presentation/imovel_card_test.dart`, `test/presentation/apresentacao_imovel_test.dart` (new); `test/presentation/cidade_selecao_screen_test.dart`, `test/presentation/seletor_cidade_topo_test.dart`, `test/main_launch_routing_test.dart` (updated for the new vitrine dependency).

Django: none in this plan.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — vitrine da cidade escolhida ponta a ponta sobre a DataSource mock (VIT-01, API-04)</name>
  <reversibility rating="reversible">The mock sits behind the ImovelDataSource interface; replacing it is one DI annotation (F1/D-14, API-04).</reversibility>
  <files>lib/domain/entities/imovel.dart, lib/domain/entities/ordenacao_vitrine.dart, lib/domain/entities/consulta_imoveis.dart, lib/domain/entities/pagina_imoveis.dart, lib/domain/repositories/imovel_repository.dart, lib/domain/usecases/buscar_imoveis_usecase.dart, lib/data/models/imovel_model.dart, lib/data/models/imoveis_envelope_model.dart, lib/data/datasources/imovel_datasource.dart, lib/data/datasources/imovel_mock_datasource.dart, lib/data/mocks/imoveis_fixture.dart, lib/data/repositories/imovel_repository_impl.dart, lib/presentation/vitrine/vitrine_state.dart, lib/presentation/vitrine/vitrine_cubit.dart, lib/presentation/vitrine/vitrine_screen.dart, lib/presentation/vitrine/widgets/imovel_card.dart, lib/presentation/cidade_selecao/cidade_selecao_screen.dart, lib/di/injection.config.dart (+ generated *.freezed.dart / *.g.dart), test/presentation/vitrine_fluxo_test.dart, test/presentation/vitrine_cubit_test.dart, test/data/imovel_repository_impl_test.dart, test/data/imovel_mock_datasource_test.dart, test/presentation/cidade_selecao_screen_test.dart, test/presentation/seletor_cidade_topo_test.dart, test/main_launch_routing_test.dart</files>
  <read_first>
    - lib/presentation/cidade_selecao/cidade_selecao_screen.dart (the `AutorizadaEAtendida` branch and `_CorpoCidadeEntrada` placeholder this task replaces; `_CorpoErro` copy pattern)
    - lib/presentation/cidade_selecao/cidade_selecao_state.dart, lib/presentation/cidade_selecao/cidade_selecao_cubit.dart (freezed sealed-union + Result switch conventions)
    - lib/presentation/cidade_selecao/widgets/seletor_cidade_topo.dart (moves to the top of the vitrine)
    - lib/core/result.dart, lib/domain/entities/cidade.dart (chaveNatural), lib/data/models/cidade_model.dart, lib/data/repositories/cidade_repository_impl.dart, lib/domain/usecases/obter_cidades_atendidas_usecase.dart (analogs)
    - test/presentation/cidade_selecao_screen_test.dart, test/presentation/seletor_cidade_topo_test.dart, test/main_launch_routing_test.dart (they render `autorizadaEAtendida` and must keep passing)
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md (§3.1, §3.2, §4, §7.2) and contrato/imoveis.example.json (exact row shape)
    - .planning/phases/02-vitrine-lista-busca-e-ordena-o/02-RESEARCH.md (Pattern 1, Pattern 2, Pattern 6, Pitfall 5, Code Examples ImovelModel/ImoveisEnvelopeModel)
    - .planning/phases/02-vitrine-lista-busca-e-ordena-o/02-PATTERNS.md (analog assignments for every new file)
  </read_first>
  <behavior>
    - Entering Campinas/SP renders the vitrine: header "Campinas, SP" and at least one ImovelCard, every rendered card's imovel.cidade == Campinas/SP (real stack, mock with zero latency).
    - Entering Indaiatuba/SP (served, no rows in the fixture) renders "Ainda não há imóveis anunciados em Indaiatuba." (D-15).
    - VitrineCubit.carregar: use case Success(non-empty) -> conteudo VitrineCarregada(itens, proximaPagina); Success(empty) with termoBusca null -> VitrineVaziaNaCidade; Success(empty) with termoBusca set -> VitrineSemResultado(termo); Failure -> VitrineErro; tentarNovamente() from VitrineErro re-runs the same consulta.
    - Stale response: carregar(Campinas) then carregar(Valinhos) before the first completes (Completer) -> only Valinhos' page is ever emitted as VitrineCarregada.
    - Closing the cubit while a request is in flight completes without throwing and emits nothing after close.
    - ImovelRepositoryImpl: DataSource envelope -> Result.success(PaginaImoveis) with entities mapped and proximaPagina == envelope.next; DataSource throwing an Exception (including FormatException for an unknown finalidade) -> Result.failure.
    - ImovelMockDataSource (paraTeste, zero latency): buscar(Campinas) returns only Campinas rows ordered by criado_em descending; buscar(Indaiatuba) returns an empty results list; response parses through ImoveisEnvelopeModel.fromJson (contract shape).
  </behavior>
  <action>
    Build the thinnest complete vertical path, in Portuguese identifiers, per D-14 / API-04 / D-13. Follow the analogs in 02-PATTERNS.md for every file (freezed abstract-class syntax with private `._()` ctor when methods are needed, `@injectable` use cases, `@LazySingleton(as: X)` impls, test-only named constructors kept OFF the injectable-scanned default constructor — Phase 1 lesson).

    Domain (plain Dart, no freezed, no package imports except `injectable` on the use case):
    `imovel.dart` defines `FinalidadeImovel`, `NaturezaImovel` and immutable `Imovel` with the fields listed in "Artifacts"; prices stay as the server's decimal strings (`precoVenda`, `precoAluguel` nullable) — the domain never parses money (D-03). `ordenacao_vitrine.dart` defines `OrdenacaoVitrine` with `valorApi` values exactly `mais_recentes`, `preco_asc`, `preco_desc`, `area_asc`, `area_desc` (D-11; PT labels live in presentation, added in plan 02-05). `consulta_imoveis.dart` holds `cidade` (required), `busca` (nullable) and `ordenacao` (default `OrdenacaoVitrine.maisRecentes`) with value equality. `pagina_imoveis.dart` holds `itens` + nullable `proximaPagina` (the opaque `next` reference, never interpreted by domain/presentation — RESEARCH Pattern 5) and `ehUltima`. `ImovelRepository` exposes `Future<Result<PaginaImoveis>> buscarImoveis(ConsultaImoveis consulta)` and `Future<Result<PaginaImoveis>> buscarProximaPagina(String proximaPagina)`. `BuscarImoveisUseCase` (`@injectable`) delegates `call(ConsultaImoveis)` and `proximaPagina(String)` straight to the repository — no logic.

    Data: `ImovelModel` (freezed + json_serializable, NON-generic per RESEARCH Pitfall 5) mirrors contract §3.1+§3.2 exactly, using `@JsonKey(name: ...)` for `preco_venda`, `preco_aluguel`, `foto_capa`, `criado_em`; `cidade` is a nested `CidadeModel`; PENDENTE-E2 fields (`natureza`, `quartos`, `suites`, `vagas`, `area`) nullable. `paraEntidade()` maps `finalidade` strings `VENDA`/`ALUGUEL`/`VENDA_E_ALUGUEL` to the enum and throws `FormatException` for any other value; `natureza` `CASA`/`APARTAMENTO`/`TERRENO`/`LOTE` maps to the enum, anything else (or null) maps to null. `ImoveisEnvelopeModel` holds `next`, `previous`, `results` and `paraPagina()`. `ImovelDataSource` is the abstract swap boundary returning `Future<ImoveisEnvelopeModel>` from `buscar(ConsultaImoveis)` and `seguir(String proximaPagina)`.
    `lib/data/mocks/imoveis_fixture.dart`: `cidadesDoFixture` = the 4 cities of contrato/cidades.example.json (ids 1-4: Campinas, Valinhos, Vinhedo, Indaiatuba, all SP); `linhasAcervoFixture()` returns, for this tracer, the 3 rows of contrato/imoveis.example.json verbatim (ids 42, 57 Campinas; 63 Valinhos) as `Map<String, Object?>` in wire shape (snake_case keys). Indaiatuba intentionally has no rows (D-15). Plan 02-03 expands this to ~40 rows per city without changing the function signature.
    `ImovelMockDataSource implements ImovelDataSource`, annotated `@LazySingleton(as: ImovelDataSource)`: default constructor uses `linhasAcervoFixture()`, latency 500 ms (D-14); `@visibleForTesting ImovelMockDataSource.paraTeste({required List<Map<String, Object?>> linhas, Duration latencia = Duration.zero})`. When latency is zero it must not schedule any timer (skip the delay entirely) so widget tests stay synchronous-friendly. `buscar` filters rows whose nested cidade matches `consulta.cidade` by `Cidade.chaveNatural` (contract §7.2 natural key, isolated here), orders by `criado_em` descending then `id` descending, and returns ONE envelope built as a wire-shape map parsed through `ImoveisEnvelopeModel.fromJson` (exercises the real parsing path), with `next` null and `previous` null for now. `busca`/`ordenacao` are accepted but not applied yet (filled in plan 02-05 without changing the interface); `seguir` throws a `FormatException` stating pagination arrives in plan 02-03 (filled there). No random-number generator and no wall-clock/current-time read anywhere in the mock or fixture (D-15) — and keep doc comments free of those API names, since plan 02-03 gates this with a negative grep that also matches comments.
    `ImovelRepositoryImpl` (`@LazySingleton(as: ImovelRepository)`) injects `ImovelDataSource` (never the mock class) and converts every `Exception` into `Result.failure`, success into `Result.success(envelope.paraPagina())` — identical shape to `CidadeRepositoryImpl.obterCidadesAtendidas`.

    Presentation: `vitrine_state.dart` (one `part` file) declares `@freezed abstract class VitrineState` with `@Default(OrdenacaoVitrine.maisRecentes) OrdenacaoVitrine ordenacao`, `String? termoBusca`, `@Default(ConteudoVitrine.carregando()) ConteudoVitrine conteudo`, and `@freezed sealed class ConteudoVitrine` with the five variants named in "Artifacts": `carregando()`, `vazioNaCidade()`, `semResultado(String termo)` (D-09, distinct from the empty city), `erro()`, and `carregada({required List<Imovel> itens, String? proximaPagina, @Default(false) bool carregandoMais, @Default(false) bool erroAoCarregarMais})` — the flattened success state of RESEARCH Pattern 1, declared in full now so plans 02-03/02-05 never reshape the union. Doc-comment each variant with the decision it serves.
    `VitrineCubit` (`@injectable`, ctor takes `BuscarImoveisUseCase`, initial `const VitrineState()`): `carregar(Cidade cidade)` stores the city and calls private `_reiniciar()`; `_reiniciar()` increments `_versaoConsulta`, emits `conteudo: carregando` (keeping ordenacao/termoBusca), awaits the use case with `ConsultaImoveis(cidade, busca: state.termoBusca, ordenacao: state.ordenacao)`, then returns early if the captured version is stale OR `isClosed` (D-13 + no emit after close), and maps the Result with an exhaustive switch exactly as listed in <behavior> (`Loading` is ignored). `tentarNovamente()` re-runs `_reiniciar()` when conteudo is `VitrineErro` (load-more retry is added in plan 02-03).
    `VitrineScreen({required Cidade cidade})` (no own Scaffold — it lives inside CidadeSelecaoScreen's Scaffold/SafeArea): a Column with `SeletorCidadeTopo(cidade: cidade)` at the top (keeps LOC-05), then an `Expanded` `BlocBuilder<VitrineCubit, VitrineState>` doing an exhaustive switch on `state.conteudo` with no default branch: carregando -> centered `CircularProgressIndicator`; vazioNaCidade -> "Ainda não há imóveis anunciados em {cidade.nome}."; semResultado -> "Nenhum imóvel encontrado para “{termo}”" (the "Limpar busca" action arrives in plan 02-05); erro -> "Não foi possível carregar os imóveis" + `TextButton` "Tentar de novo" calling `tentarNovamente()`; carregada -> `ListView.builder` of `ImovelCard(key: ValueKey('imovel-${imovel.id}'), imovel: ...)`. Texts use `Theme.of(context).textTheme` roles from 01-UI-SPEC (titleLarge heading, bodyLarge body, labelLarge buttons).
    `ImovelCard({required Imovel imovel})` in this task: minimal Material 3 `Card` showing `titulo` and `bairro` only (Task 2 completes the layout in the same file).
    `cidade_selecao_screen.dart`: add optional ctor param `VitrineCubit Function()? criarVitrineCubit` (same optional-injection style as `salvarCidade`, defaulting to `() => getIt<VitrineCubit>()`); replace the `_CorpoCidadeEntrada` placeholder in the `AutorizadaEAtendida` branch with a private widget returning `BlocProvider<VitrineCubit>(key: ValueKey(cidade.chaveNatural), create: (_) => criar()..carregar(cidade), child: VitrineScreen(cidade: cidade))`. Delete the now-unused `_CorpoCidadeEntrada`.
    Run `dart run build_runner build --delete-conflicting-outputs` to generate freezed/json parts and the updated `injection.config.dart`; the build must report no missing-dependency warning.

    Tests (write them first — tdd): `test/presentation/vitrine_fluxo_test.dart` is the END-TO-END check: pump `CidadeSelecaoScreen(salvarCidade: <mock>, criarVitrineCubit: () => VitrineCubit(BuscarImoveisUseCase(ImovelRepositoryImpl(ImovelMockDataSource.paraTeste(linhas: linhasAcervoFixture())))))` under a `MockCubit<CidadeSelecaoState>` in `autorizadaEAtendida(Campinas/SP)`; assert the behaviors for Campinas and Indaiatuba (use `pump()` twice, never `pumpAndSettle` while the spinner can be visible). `test/presentation/vitrine_cubit_test.dart` (bloc_test + mocktail on `BuscarImoveisUseCase`, `registerFallbackValue` for `ConsultaImoveis`) covers the carregar/tentarNovamente/stale/close behaviors. `test/data/imovel_repository_impl_test.dart` mocks `ImovelDataSource`. `test/data/imovel_mock_datasource_test.dart` covers the mock behaviors.
    Update the three existing tests that render `autorizadaEAtendida`: give `CidadeSelecaoScreen` a `criarVitrineCubit` returning a `MockCubit<VitrineState>` fake (stub `carregar(any())`, `registerFallbackValue` for `Cidade`) whose `whenListen` initial state is `VitrineState(conteudo: ConteudoVitrine.vazioNaCidade())` — a NON-animating state, because those tests call `pumpAndSettle()` and an indeterminate spinner would time out; in `test/main_launch_routing_test.dart` register the fake with `getIt.registerFactory<VitrineCubit>`. Their existing assertions ("Campinas, SP" header, switcher tap reopens the list without GPS, selecting Valinhos persists) must keep passing unchanged.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs</automated>
    <fails_when>non-zero exit, or "[SEVERE]" in the output, or a "Missing dependencies" / "unregistered type" warning line</fails_when>
    <automated>flutter test test/presentation/vitrine_fluxo_test.dart test/presentation/vitrine_cubit_test.dart test/data/imovel_repository_impl_test.dart test/data/imovel_mock_datasource_test.dart</automated>
    <fails_when>non-zero exit, or "Some tests failed" in the output, or "No tests ran"</fails_when>
    <automated>flutter test</automated>
    <fails_when>non-zero exit, or "Some tests failed" in the output</fails_when>
    <automated>flutter analyze</automated>
    <fails_when>non-zero exit, or "issues found" in the output (infos are fatal by default)</fails_when>
    <automated>! grep -rln "ImovelMockDataSource" lib/presentation lib/domain lib/data/repositories</automated>
    <fails_when>any file path is printed (the mock class leaked past the ImovelDataSource boundary)</fails_when>
    <automated>! grep -rn "package:dio" lib/presentation lib/domain</automated>
    <fails_when>any matching line is printed (a widget/Cubit/domain file imports the HTTP client)</fails_when>
    <automated>! grep -rn "catch" lib/presentation</automated>
    <fails_when>any matching line is printed (generic error handling leaked into the presentation layer)</fails_when>
  </verify>
  <!-- planner-discipline-allow: ImovelMockDataSource -->
  <acceptance_criteria>
    - `test/presentation/vitrine_fluxo_test.dart` passes through the REAL Cubit/UseCase/Repository/mock stack: Campinas renders only Campinas cards under the "Campinas, SP" header; Indaiatuba renders "Ainda não há imóveis anunciados em Indaiatuba.".
    - `test/presentation/vitrine_cubit_test.dart` proves: success -> VitrineCarregada; empty -> VitrineVaziaNaCidade; failure -> VitrineErro; tentarNovamente re-runs; a stale Campinas response arriving after carregar(Valinhos) is never emitted; close() during an in-flight request throws nothing.
    - `test/data/imovel_repository_impl_test.dart` proves Exception (incl. FormatException for unknown finalidade) -> Result.failure and envelope -> PaginaImoveis mapping.
    - `lib/data/repositories/imovel_repository_impl.dart` contains `ImovelDataSource` and `grep -rln "ImovelMockDataSource" lib/presentation lib/domain lib/data/repositories` prints nothing.
    - `lib/data/datasources/imovel_mock_datasource.dart` contains `@LazySingleton(as: ImovelDataSource)` and no `Random` / `DateTime.now`.
    - `lib/presentation/cidade_selecao/cidade_selecao_screen.dart` contains `VitrineScreen` and `criarVitrineCubit`, and no longer contains `_CorpoCidadeEntrada`.
    - Full `flutter test` green (all Phase 1 tests still pass) and `flutter analyze` reports no issues.
  </acceptance_criteria>
  <done>A visitor entering a served city sees that city's imóveis from the mock through every layer, the empty/error paths render their own states, the three legacy tests still pass, and the tracer is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Card completo do imóvel — foto 16:9 com placeholder, preços BRL, natureza, bairro, quartos (VIT-02)</name>
  <files>pubspec.yaml, pubspec.lock, lib/presentation/vitrine/apresentacao_imovel.dart, lib/presentation/vitrine/widgets/foto_capa_imovel.dart, lib/presentation/vitrine/widgets/imovel_card.dart, test/presentation/apresentacao_imovel_test.dart, test/presentation/imovel_card_test.dart</files>
  <read_first>
    - lib/presentation/vitrine/widgets/imovel_card.dart and lib/domain/entities/imovel.dart (from Task 1)
    - pubspec.yaml (current dependency block)
    - .planning/phases/02-vitrine-lista-busca-e-ordena-o/02-CONTEXT.md (D-01, D-02, D-03, D-04)
    - .planning/phases/02-vitrine-lista-busca-e-ordena-o/02-RESEARCH.md (Standard Stack; Package Legitimacy Audit; Pitfall 6 AspectRatio; Pitfall 7 decimalDigits)
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-UI-SPEC.md (Typography roles, Spacing scale, Color roles — primaryContainer is the light-green tonal surface)
  </read_first>
  <behavior>
    - formatarPrecoBrl("450000.00") == "R$ 450.000"; formatarPrecoBrl("450000.50") == "R$ 450.000,50"; formatarPrecoBrl("abc") == "abc" (never throws). intl's pt_BR pattern puts a NO-BREAK SPACE (U+00A0) between "R$" and the digits — assert with " ", not a plain space.
    - linhasDePreco: VENDA -> ["R$ 450.000"]; ALUGUEL -> ["R$ 3.200/mês"]; VENDA_E_ALUGUEL -> ["Venda R$ 220.000", "Aluguel R$ 1.500/mês"]; a finalidade whose price is null -> that line omitted; no line at all -> ["Preço sob consulta"].
    - rotuloNatureza: casa->"Casa", apartamento->"Apartamento", terreno->"Terreno", lote->"Lote", null->null. rotuloQuartos: null or 0 -> null, 1 -> "1 quarto", 3 -> "3 quartos".
    - ImovelCard renders, in this order: FotoCapaImovel, título, price line(s), then natureza · bairro · quartos; fotoCapa null shows the placeholder (Icons.home_outlined on primaryContainer) inside an AspectRatio of 16/9.
    - The photo slot height is identical for a card with fotoCapa null and a card with a URL on the first frame (same AspectRatio for placeholder, loading and error).
  </behavior>
  <action>
    Add dependencies with `flutter pub add intl cached_network_image` and `flutter pub add --dev fake_async` (versions verified in RESEARCH's Package Legitimacy Audit: intl ^0.20.3, cached_network_image ^4.0.2, fake_async ^1.3.3 — all OK/Approved; fake_async is added now so the debounce tests of plan 02-05 need no further pubspec change). Keep `pubspec.lock` committed.

    Create `lib/presentation/vitrine/apresentacao_imovel.dart` — pure top-level presentation functions, no widgets, per D-03 ("conversão string->número/format fica fora do widget"): `String formatarPrecoBrl(String valorDecimal)` parses with `double.tryParse` (unparseable -> return the input unchanged), picks `decimalDigits: 0` when the cents are zero and `2` otherwise (RESEARCH Pitfall 7), and formats with `NumberFormat.currency(locale: 'pt_BR', symbol: 'R\$', ...)`; `List<String> linhasDePreco(Imovel imovel)` implements D-02 exactly as in <behavior> (rent suffix "/mês"; "Venda "/"Aluguel " prefixes only for VENDA_E_ALUGUEL); `String? rotuloNatureza(NaturezaImovel? natureza)`; `String? rotuloQuartos(int? quartos)`. These only present server values — no price is computed.

    Create `lib/presentation/vitrine/widgets/foto_capa_imovel.dart`: `FotoCapaImovel({required String? url})` wraps EVERYTHING in one `AspectRatio(aspectRatio: 16 / 9)` (RESEARCH Pitfall 6); null url -> placeholder; otherwise `CachedNetworkImage` with `fit: BoxFit.cover` whose `placeholder` and `errorWidget` are both the same placeholder (D-04). Placeholder = `ColoredBox` in `colorScheme.primaryContainer` with a centered `Icons.home_outlined` (size 48, `onPrimaryContainer`). If `CachedNetworkImage` raises plugin errors (path_provider) inside `flutter test`, add an optional `@visibleForTesting` constructor parameter that lets tests supply the network-image builder, keeping production on `CachedNetworkImage`.

    Complete `lib/presentation/vitrine/widgets/imovel_card.dart` (D-01): Material 3 `Card` with `clipBehavior: Clip.antiAlias`, a Column with `FotoCapaImovel(url: imovel.fotoCapa)` on top, then 16px padding (UI-SPEC md) holding: título (`titleMedium`, `maxLines: 2`, ellipsis — long-text backstop), the price lines from `linhasDePreco` (`titleMedium`, color `colorScheme.primary`), and one meta line joining the non-null `rotuloNatureza`, `bairro` and `rotuloQuartos` with " · " (`bodyMedium`, `maxLines: 1`, ellipsis). Same slot order in every card (VIT-02). No tap handler (detail page is DET-01, v2). No network or business logic in the widget.

    Tests first: `test/presentation/apresentacao_imovel_test.dart` (pure unit, every <behavior> formatting case) and `test/presentation/imovel_card_test.dart` (widget: VENDA card shows título, "R$ 450.000", "Apartamento · Cambuí · 2 quartos"; VENDA_E_ALUGUEL card shows both stacked lines; null photo shows `Icons.home_outlined` inside an `AspectRatio` whose `aspectRatio` is 16/9; photo-slot height equal for null vs URL on the first frame — use `pump()`, never `pumpAndSettle`).
  </action>
  <verify>
    <automated>flutter test test/presentation/apresentacao_imovel_test.dart test/presentation/imovel_card_test.dart</automated>
    <fails_when>non-zero exit, or "Some tests failed" in the output, or "No tests ran"</fails_when>
    <automated>flutter test</automated>
    <fails_when>non-zero exit, or "Some tests failed" in the output</fails_when>
    <automated>flutter analyze</automated>
    <fails_when>non-zero exit, or "issues found" in the output</fails_when>
    <automated>grep -nE "^\s+(intl|cached_network_image|fake_async):" pubspec.yaml</automated>
    <fails_when>fewer than three lines printed, or non-zero exit</fails_when>
    <human-check>
      <test>At end of phase, with the Django dev server from plan 02-02 running (cities come from the API after plan 02-04), run `flutter run` on an Android emulator and an iOS simulator and enter Campinas/SP.</test>
      <expected>Cards are one per row, wide photo on top with equal height across cards (photo-less rows show the light-green house placeholder), título, price in "R$ 450.000" style (no ",00"), rent with "/mês", the VENDA_E_ALUGUEL row shows two stacked prices, and natureza · bairro · quartos under it.</expected>
      <why_human>Visual consistency of card heights and real image loading/caching cannot be asserted by widget tests (network images are disabled in flutter test).</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `test/presentation/apresentacao_imovel_test.dart` asserts "R$ 450.000", "R$ 450.000,50", the "/mês" suffix, the stacked VENDA_E_ALUGUEL lines and the unparseable passthrough.
    - `test/presentation/imovel_card_test.dart` asserts the slot order, the placeholder icon `Icons.home_outlined` inside a 16/9 `AspectRatio`, and equal photo-slot height for null vs URL.
    - `lib/presentation/vitrine/widgets/imovel_card.dart` contains `linhasDePreco` and `FotoCapaImovel`; `lib/presentation/vitrine/apresentacao_imovel.dart` contains `NumberFormat.currency` and `pt_BR`.
    - `pubspec.yaml` declares `intl`, `cached_network_image` (dependencies) and `fake_async` (dev_dependencies); `flutter test` and `flutter analyze` are clean.
  </acceptance_criteria>
  <done>Every vitrine card shows the full VIT-02 layout with consistent photo height and correctly formatted BRL prices, covered by unit and widget tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/ -> presentation/ | Imóvel rows (today from the in-app mock, Phase 4 from the public API) cross into widgets; strings are rendered, never executed |
| app -> image hosts | `foto_capa` URLs are fetched by `cached_network_image` |
| pub.dev -> build | New packages intl, cached_network_image, fake_async enter the dependency graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-01 | Tampering | ImovelModel.paraEntidade | low | mitigate | Unknown `finalidade` throws FormatException -> repository returns Result.failure -> VitrineErro state; unknown `natureza` maps to null; prices are never parsed in domain (D-03) |
| T-02-01-02 | Denial of Service | FotoCapaImovel / ImovelCard | low | mitigate | Unparseable price strings are rendered as-is (formatarPrecoBrl never throws); image load failure falls back to the placeholder inside a fixed AspectRatio, so a bad URL cannot break the list layout |
| T-02-01-03 | Information Disclosure | VitrineCubit / widgets | low | mitigate | UI/Cubit only ever receive one page of already-filtered rows; the full acervo stays inside ImovelMockDataSource (verified by the negative grep on lib/presentation, lib/domain, lib/data/repositories) |
| T-02-01-04 | Denial of Service | VitrineCubit lifecycle | low | mitigate | `isClosed` + version-token guard before every emit — switching city with a request in flight cannot throw "emit after close" |
| T-02-01-SC | Tampering | pub packages intl, cached_network_image, fake_async | medium | mitigate | RESEARCH Package Legitimacy Audit rated all three OK/Approved (publishers dart.dev / baseflow.com, verified on the pub.dev registry); caret constraints recorded in pubspec.yaml and resolved versions pinned in the committed pubspec.lock |
</threat_model>

<verification>
- `flutter test` (full suite) green, `flutter analyze` clean, `dart run build_runner build --delete-conflicting-outputs` without warnings.
- Negative greps: no `ImovelMockDataSource` outside data/datasources + DI; no `package:dio` in presentation/domain; no `catch` in presentation.
- End-to-end widget test drives the real stack from CidadeSelecaoScreen to rendered ImovelCards.
</verification>

<success_criteria>
- A visitor who enters a served city sees that city's imóveis in full VIT-02 cards (VIT-01, VIT-02).
- The imóveis data layer is behind `ImovelDataSource`; the repository never names the mock (API-04).
- Empty city and first-page failure each have their own state; stale responses and post-close emits are impossible (D-13, D-15).
</success_criteria>

<output>
Create `.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-01-SUMMARY.md` when done
</output>
