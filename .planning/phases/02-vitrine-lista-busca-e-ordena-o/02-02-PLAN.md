---
phase: 02-vitrine-lista-busca-e-ordena-o
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ../imoveis-aqui/Web/empresas/api/views.py
  - ../imoveis-aqui/Web/empresas/api/urls_publico.py
  - ../imoveis-aqui/Web/empresas/tests.py
  - ../imoveis-aqui/Web/empresas/management/__init__.py
  - ../imoveis-aqui/Web/empresas/management/commands/__init__.py
  - ../imoveis-aqui/Web/empresas/management/commands/semear_vitrine_dev.py
  - ../imoveis-aqui/Web/.env.example
  - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md
autonomous: true
requirements: [API-02]
user_setup:
  - service: django-dev-server (../imoveis-aqui/Web)
    why: "Verificação manual de GET /api/publico/cidades/ e do app contra o endpoint real (API-02, VIT-06). Os testes automatizados usam SQLite em memória e não precisam disto."
    env_vars:
      - name: DATABASE_URL
        source: "Web/.env (copiar de Web/.env.example) — credenciais do seu Postgres local; o Claude não conhece a senha (a autenticação com postgres:postgres falhou nesta máquina)"
      - name: ALLOWED_HOSTS
        source: "Web/.env — incluir 10.0.2.2 (emulador Android) e o IP da rede local (aparelho físico), além de localhost,127.0.0.1"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /api/publico/cidades/ responde 200 sem token nem sessão, no envelope cursor {next, previous, results} do contrato §2.2/§4 (API-02, F1/D-01)."
    - "O endpoint lista somente cidades com pelo menos uma Empresa ativa atuando nelas — cidade sem empresa ou só com empresa inativa nunca aparece; cidade com duas empresas ativas aparece uma única vez (contrato §2.3, ASVS V4)."
    - "Cada linha expõe exatamente os campos id, nome e uf (CidadeSerializer reaproveitado, allowlist do contrato §2.1) — nenhum dado de empresa ou interno."
    - "A paginação é CursorPagination com page_size fixo 50 e ordering (nome, uf); o 'next' é uma URL absoluta e seguir as páginas até next == null devolve cada cidade exatamente uma vez, mesmo se uma cidade for cadastrada entre duas requisições (edge API-02 concorrência)."
    - "O endpoint é somente leitura: POST/PUT/DELETE respondem 405."
    - "O comando de desenvolvimento semear_vitrine_dev cria (idempotente, rodar duas vezes não duplica nada) as mesmas 4 cidades de contrato/cidades.example.json e da fixture mock do app — Campinas, Valinhos, Vinhedo, Indaiatuba (SP) — atendidas por uma Empresa ativa de demonstração, e recusa rodar com DEBUG=False (RESEARCH Pitfall 4 / A3)."
    - "Nenhum commit é feito no repositório irmão ../imoveis-aqui sem autorização explícita do usuário (CLAUDE.md do repo irmão): as mudanças ficam no branch feat/APP02, não commitadas, aguardando o aval."
    - "O contrato 01-CONTRATO-API.md registra, como adendos aguardando o sign-off do E2, o param busca (D-05/D-06), os valores de ordenacao adotados (D-11), a regra de preço-base com nulls last (D-12) e a limitação do CursorPagination do DRF com campos nulos (RESEARCH Pitfall 3)."
  artifacts:
    - path: "../imoveis-aqui/Web/empresas/api/views.py"
      provides: "CidadeCursorPagination + CidadesPublicasAPIView (AllowAny, ListAPIView)"
      contains: "class CidadesPublicasAPIView"
    - path: "../imoveis-aqui/Web/empresas/api/urls_publico.py"
      provides: "rota api/publico/cidades/"
      contains: "publico-cidades"
    - path: "../imoveis-aqui/Web/empresas/management/commands/semear_vitrine_dev.py"
      provides: "seed idempotente das 4 cidades atendidas para o ambiente de desenvolvimento"
      contains: "get_or_create"
    - path: "../imoveis-aqui/Web/empresas/tests.py"
      provides: "CidadesPublicasAPITests + SemearVitrineDevTests"
      contains: "class CidadesPublicasAPITests"
    - path: ".planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md"
      provides: "adendos da Fase 2 ao contrato"
      contains: "Adendos da Fase 2"
  key_links:
    - from: "../imoveis-aqui/Web/empresas/api/urls_publico.py"
      to: "../imoveis-aqui/Web/empresas/api/views.py"
      via: "path('cidades/', CidadesPublicasAPIView.as_view()) sob o include api/publico/ já existente em config/urls.py"
      pattern: "CidadesPublicasAPIView"
    - from: "../imoveis-aqui/Web/empresas/api/views.py"
      to: "../imoveis-aqui/Web/empresas/api/serializers.py"
      via: "serializer_class = CidadeSerializer (reaproveitado sem alteração)"
      pattern: "CidadeSerializer"
    - from: "../imoveis-aqui/Web/empresas/api/views.py"
      to: "../imoveis-aqui/Web/empresas/models.py"
      via: "queryset filtrado no banco por empresas_atuantes__ativa=True"
      pattern: "empresas_atuantes__ativa=True"
  prohibitions:
    - "O endpoint público de cidades nunca decide o conjunto 'atendidas' por parâmetro vindo do cliente nem expõe campos além de id/nome/uf — a regra vive no queryset do servidor (contrato §2.3/§6)."
    - "O comando de seed nunca roda fora de DEBUG=True e nunca altera uma Empresa real existente (busca a Empresa de demonstração por um nome marcador próprio)."
    - "Nada é commitado no repositório irmão sem a autorização explícita do usuário."
---

<objective>
Create the real public endpoint `GET /api/publico/cidades/` in the sibling Django repo (API-02),
with DRF tests and an idempotent dev seed that serves the same 4 cities the app's mock uses, and
record this phase's contract addenda (D-05 `busca`, D-11 `ordenacao` values, D-12 price base
nulls-last + the DRF CursorPagination limitation) in the frozen contract document.

Purpose: VIT-06 (plan 02-04) needs a real, public, served-cities-only endpoint to consume; the
addenda keep the contract the single source of truth before Phase 4 builds `/imoveis`.
Output: endpoint + route + tests + seed command on branch `feat/APP02` (uncommitted, awaiting the
user's authorization) and the updated `01-CONTRATO-API.md`.
</objective>

<user_story>
**As a** visitante do app (e do site), **I want to** que a lista de cidades venha do servidor com
só as cidades realmente atendidas, **so that** eu nunca escolho uma cidade sem imobiliária ativa e o
app e o site mostram o mesmo conjunto.
</user_story>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-CONTEXT.md
@.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-RESEARCH.md
@.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-PATTERNS.md
@.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md
@.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/cidades.example.json
@../imoveis-aqui/CLAUDE.md
</context>

## Artifacts this phase produces (share delivered by this plan)

Django (sibling repo `../imoveis-aqui/Web`, branch `feat/APP02`, uncommitted):
- `empresas/api/views.py` — `class CidadeCursorPagination(CursorPagination)` (`page_size = 50`, `ordering = ("nome", "uf")`, `cursor_query_param = "cursor"`); `class CidadesPublicasAPIView(generics.ListAPIView)` (`permission_classes = [AllowAny]`, `serializer_class = CidadeSerializer`, `pagination_class = CidadeCursorPagination`, `queryset = Cidade.objects.filter(empresas_atuantes__ativa=True).distinct()`).
- `empresas/api/urls_publico.py` — `path("cidades/", CidadesPublicasAPIView.as_view(), name="publico-cidades")`.
- `empresas/management/__init__.py`, `empresas/management/commands/__init__.py` (new, empty packages).
- `empresas/management/commands/semear_vitrine_dev.py` — `class Command(BaseCommand)`, constants `CIDADES_VITRINE` and `NOME_EMPRESA_DEMO`.
- `empresas/tests.py` — `class CidadesPublicasAPITests(APITestCase)`, `class SemearVitrineDevTests(TestCase)`.
- `.env.example` — `ALLOWED_HOSTS` line extended with `10.0.2.2` + comment about the LAN IP.

App repo (docs):
- `.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md` — `busca` row in §5, §7.4 note, new §7.5 (preço-base + nulls last + DRF limitation), §2.2/§2.3 notes, new `## 9. Adendos da Fase 2`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/publico/cidades/ público, só cidades atendidas, cursor + seed de desenvolvimento (API-02) — no repo irmão, sem commit</name>
  <precondition>`git -C ../imoveis-aqui status --porcelain` prints nothing (clean tree) and `../imoveis-aqui/Web/.venv/bin/python` exists; if the tree is dirty, stop and ask the user instead of switching branches.</precondition>
  <files>../imoveis-aqui/Web/empresas/api/views.py, ../imoveis-aqui/Web/empresas/api/urls_publico.py, ../imoveis-aqui/Web/empresas/tests.py, ../imoveis-aqui/Web/empresas/management/__init__.py, ../imoveis-aqui/Web/empresas/management/commands/__init__.py, ../imoveis-aqui/Web/empresas/management/commands/semear_vitrine_dev.py, ../imoveis-aqui/Web/.env.example</files>
  <read_first>
    - ../imoveis-aqui/CLAUDE.md (no commit/push without the user's explicit authorization; explain in Portuguese before editing several files; Portuguese naming)
    - ../imoveis-aqui/Web/empresas/api/views.py (EmpresaPublicaAPIView — the AllowAny precedent to copy)
    - ../imoveis-aqui/Web/empresas/api/urls_publico.py, ../imoveis-aqui/Web/config/urls.py (api/publico/ include already exists)
    - ../imoveis-aqui/Web/empresas/api/serializers.py (CidadeSerializer, reused as-is)
    - ../imoveis-aqui/Web/empresas/models.py (Empresa.cidades_atuacao related_name="empresas_atuantes", ativa, required fields), ../imoveis-aqui/Web/localizacao/models.py (Cidade, Meta.ordering)
    - ../imoveis-aqui/Web/core/validators.py (validar_cnpj — to pick a valid demo CNPJ)
    - ../imoveis-aqui/Web/empresas/tests.py, ../imoveis-aqui/Web/config/settings.py (REST_FRAMEWORK defaults: IsAuthenticated global, no PAGE_SIZE), ../imoveis-aqui/Web/.env.example
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md (§2.1-§2.3, §4) and contrato/cidades.example.json
    - .planning/phases/02-vitrine-lista-busca-e-ordena-o/02-RESEARCH.md (Code Example "Django — endpoint público de cidades", Pitfall 4, Security Domain)
  </read_first>
  <behavior>
    - GET /api/publico/cidades/ without credentials -> 200 with keys exactly next, previous, results.
    - Served-only: cities Campinas (2 active companies) and Valinhos (1 active) listed once each, ordered by nome; Sorocaba (no company) and Jundiaí (only an inactive company) absent.
    - Each row has exactly the keys id, nome, uf.
    - 55 served cities -> first page 50 rows and an absolute next URL containing "cursor="; following next returns the remaining 5 with next == null; union of ids == all 55, no duplicates; creating a new served city between the two requests does not duplicate any page-1 id on page 2.
    - POST to the endpoint -> 405.
    - call_command("semear_vitrine_dev") twice under DEBUG=True -> exactly the 4 contract cities exist once each, exactly one demo Empresa (ativa=True) acts in all 4, and the endpoint lists them; under DEBUG=False -> CommandError and nothing created.
  </behavior>
  <action>
    Before editing, state in Portuguese (in your run output) which files you will change, as the sibling repo's CLAUDE.md requires. Create branch `feat/APP02` from `main` in `../imoveis-aqui` (`git -C ../imoveis-aqui switch -c feat/APP02`; reuse it if it already exists). DO NOT run `git commit`/`git push` in `../imoveis-aqui` at any point — its CLAUDE.md forbids committing without the user's explicit authorization; leave every change uncommitted on `feat/APP02` and record that in the SUMMARY (the app-repo task commit for this task is empty and may be skipped).

    In `empresas/api/views.py` (same file as the `EmpresaPublicaAPIView` precedent, per 02-PATTERNS.md) add `CidadeCursorPagination(CursorPagination)` with `page_size = 50` (MANDATORY: the project has no REST_FRAMEWORK PAGE_SIZE, and a CursorPagination with page_size None silently returns an unpaginated flat list instead of the contract envelope), `ordering = ("nome", "uf")` (same as `Cidade.Meta.ordering`, stable and non-null — contract §4) and `cursor_query_param = "cursor"`; leave `page_size_query_param` unset so clients cannot enlarge pages. Add `CidadesPublicasAPIView(generics.ListAPIView)` with `permission_classes = [AllowAny]`, `serializer_class = CidadeSerializer` (imported from `.serializers`, unchanged), `pagination_class = CidadeCursorPagination`, `queryset = Cidade.objects.filter(empresas_atuantes__ativa=True).distinct()` (contract §2.3 — decided in the queryset, never from a request parameter), and a Portuguese docstring. Register `path("cidades/", CidadesPublicasAPIView.as_view(), name="publico-cidades")` in `empresas/api/urls_publico.py`; `config/urls.py` needs no change.

    Create the dev seed `empresas/management/commands/semear_vitrine_dev.py` (plus empty `empresas/management/__init__.py` and `empresas/management/commands/__init__.py`): `CIDADES_VITRINE` = Campinas, Valinhos, Vinhedo, Indaiatuba (all "SP") — exactly contrato/cidades.example.json and the app mock fixture (RESEARCH Pitfall 4 / A3); `NOME_EMPRESA_DEMO` = "Imobiliária Vitrine Demo (dev)". `handle()` raises `CommandError` unless `settings.DEBUG` is true; otherwise `get_or_create`s each city by (nome, uf), `get_or_create`s the demo Empresa by `razao_social_ou_nome=NOME_EMPRESA_DEMO` (defaults: tipo IMOBILIARIA, a CNPJ that passes `core.validators.validar_cnpj` and is not the one used in existing tests, creci, whatsapp, email `vitrine-demo@example.com`, ativa True), adds the 4 cities to its `cidades_atuacao`, and prints a Portuguese summary. Idempotent: running twice creates nothing new. It never looks up or modifies any other Empresa.

    Tests (write first) in `empresas/tests.py`: `CidadesPublicasAPITests(APITestCase)` covering every endpoint <behavior> (use `reverse("publico-cidades")` and the `rest_framework.test` client with no credentials), and `SemearVitrineDevTests(TestCase)` using `call_command` with `override_settings(DEBUG=True)` / `DEBUG=False` (the test runner forces DEBUG False by default). Create Empresa rows with `Empresa.objects.create` and distinct cnpj values.

    In `.env.example` extend the `ALLOWED_HOSTS` line to `localhost,127.0.0.1,10.0.2.2` with a Portuguese comment: 10.0.2.2 is the Android emulator's alias for the host machine; add the machine's LAN IP when testing on a physical device; run `python manage.py runserver 0.0.0.0:8000`. Do not touch `.gitignore` or `config/settings.py`.

    Run the suite with `DATABASE_URL="sqlite://:memory:"` — verified in this environment: the default Postgres credentials are rejected and there is no `Web/.env`; the env var override only affects this process (django-environ reads process env before `.env`), it does not change settings.
  </action>
  <verify>
    <automated>cd ../imoveis-aqui/Web && DATABASE_URL="sqlite://:memory:" .venv/bin/python manage.py test empresas --noinput</automated>
    <fails_when>non-zero exit, or "FAILED (" in the output, or "Ran 0 tests"</fails_when>
    <automated>cd ../imoveis-aqui/Web && DATABASE_URL="sqlite://:memory:" .venv/bin/python manage.py check</automated>
    <fails_when>non-zero exit, or the line "System check identified no issues" is absent</fails_when>
    <automated>test "$(git -C ../imoveis-aqui rev-parse --abbrev-ref HEAD)" = "feat/APP02" && test "$(git -C ../imoveis-aqui rev-parse HEAD)" = "$(git -C ../imoveis-aqui rev-parse main)"</automated>
    <fails_when>non-zero exit (wrong branch, or a commit was created in the sibling repo without authorization)</fails_when>
    <automated>grep -n "page_size = 50" ../imoveis-aqui/Web/empresas/api/views.py && grep -n "empresas_atuantes__ativa=True" ../imoveis-aqui/Web/empresas/api/views.py && grep -n "publico-cidades" ../imoveis-aqui/Web/empresas/api/urls_publico.py</automated>
    <fails_when>non-zero exit (one of the three required lines is missing)</fails_when>
    <human-check>
      <test>In ../imoveis-aqui/Web with your own .env (DATABASE_URL for your Postgres, ALLOWED_HOSTS including 10.0.2.2): `python manage.py migrate`, `python manage.py semear_vitrine_dev`, `python manage.py runserver 0.0.0.0:8000`, then `curl -i http://localhost:8000/api/publico/cidades/` and `curl -i -H "Host: 10.0.2.2:8000" http://localhost:8000/api/publico/cidades/`.</test>
      <expected>Both return HTTP 200 without any token, body {"next": null, "previous": null, "results": [...]} containing Campinas, Indaiatuba, Valinhos, Vinhedo (SP) ordered by nome, each with only id/nome/uf.</expected>
      <why_human>Requires the user's Postgres credentials (unknown to Claude) and a running dev server.</why_human>
    </human-check>
    <human-check>
      <test>Review `git -C ../imoveis-aqui diff` and `git -C ../imoveis-aqui status` on branch feat/APP02.</test>
      <expected>Only the files listed in this plan changed; you explicitly authorize (or perform) the commit on feat/APP02 — Claude did not commit in the sibling repo.</expected>
      <why_human>The sibling repo's CLAUDE.md requires the user's explicit authorization for every commit.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `empresas/tests.py` contains `class CidadesPublicasAPITests` and `class SemearVitrineDevTests`, and the Django test run for `empresas` passes on in-memory SQLite.
    - `empresas/api/views.py` contains `class CidadeCursorPagination`, `page_size = 50`, `ordering = ("nome", "uf")`, `class CidadesPublicasAPIView`, `AllowAny`, `CidadeSerializer`, `empresas_atuantes__ativa=True` and `.distinct()`.
    - `empresas/api/urls_publico.py` contains `name="publico-cidades"`.
    - `empresas/management/commands/semear_vitrine_dev.py` contains `CommandError`, `settings.DEBUG`, `get_or_create`, and the four names Campinas, Valinhos, Vinhedo, Indaiatuba.
    - `.env.example` ALLOWED_HOSTS line contains `10.0.2.2`.
    - `../imoveis-aqui` is on branch `feat/APP02` and its HEAD still equals `main` (no unauthorized commit).
  </acceptance_criteria>
  <done>The public cities endpoint exists with served-only, allowlisted, cursor-paginated output, an idempotent DEBUG-only seed provides the 4 shared cities, all Django tests pass, and the changes await the user's commit authorization on feat/APP02.</done>
</task>

<task type="auto">
  <name>Task 2: Adendos da Fase 2 no contrato congelado — busca, ordenacao, preço-base nulls last, paginação de /cidades</name>
  <files>.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md</files>
  <read_first>
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md (whole file; §2.2, §2.3, §4, §5, §7, §8)
    - .planning/phases/02-vitrine-lista-busca-e-ordena-o/02-CONTEXT.md (D-05, D-06, D-11, D-12, D-16, "Specific Ideas" — three addenda for E2 sign-off)
    - .planning/phases/02-vitrine-lista-busca-e-ordena-o/02-RESEARCH.md (Pitfall 3 — DRF CursorPagination requires a non-nullable ordering field; Open Question 1)
  </read_first>
  <action>
    Edit `01-CONTRATO-API.md` in place (scoped edits, keep every existing section; Portuguese prose; every new item marked "adendo Fase 2 — aguardando sign-off do E2"):
    (1) §5 table: add a row for the `busca` param — string; server-side text search over `titulo` + `endereco__bairro`, case/accent-insensitive as far as the backend allows, `descricao` excluded (D-05, D-06); Django hint `SearchFilter` with `search_param = "busca"`.
    (2) §7.4: note that the proposed values `mais_recentes` (default, equivalent to ordering `-criado_em`), `preco_asc`, `preco_desc`, `area_asc`, `area_desc` were adopted as WORKING values by the app in Phase 2 (D-11), still pending E2 approval; the app keeps them in the domain enum `OrdenacaoVitrine.valorApi`.
    (3) New §7.5 "Preço-base da ordenação e nulos": price ordering uses `preco_venda`; rows without `preco_venda` (rent-only) go LAST in both directions (nulls last); `area_*` with null `area` also nulls last; in Phase 3, with `finalidade=ALUGUEL`, price ordering switches to `preco_aluguel` (D-12). Record the open technical risk (RESEARCH Pitfall 3): DRF `CursorPagination` requires a non-nullable ordering field and re-applies its own ordering over any custom `OrderingFilter` (DRF docs, discussion encode/django-rest-framework#9456); list the options for E2 to decide before Phase 4 (annotate with `Coalesce` to a documented sentinel vs. another approach) without choosing one.
    (4) §2.2 or §4: note that `/cidades` uses `page_size = 50`, `ordering ("nome", "uf")`, that `next`/`previous` are absolute URLs, and that the app follows `next` until null (RESEARCH Pattern 5, A2).
    (5) §2.3: note that since Phase 2 the app's bundled city list was removed (D-16) — the endpoint is the single source — and that the dev seed command `semear_vitrine_dev` (repo irmão) seeds the same 4 cities as contrato/cidades.example.json and the app's mock fixture (Pitfall 4).
    (6) Append `## 9. Adendos da Fase 2 (aguardando sign-off do E2)` summarising items 1-3 plus the `cidade` param format the mock uses (`Campinas-SP`, following the provisional §7.2 recommendation, still pending), and stating that the §8 sync/sign-off process applies to them.
  </action>
  <verify>
    <automated>grep -n "Adendos da Fase 2" .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md && grep -n "search_param" .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md && grep -n "Coalesce" .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md && grep -n "semear_vitrine_dev" .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md && grep -n "page_size = 50" .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md</automated>
    <fails_when>non-zero exit (one of the five required addendum markers is missing)</fails_when>
    <automated>grep -c "^## " .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md</automated>
    <fails_when>a count below 9 (an existing top-level section was lost or §9 was not added)</fails_when>
  </verify>
  <acceptance_criteria>
    - §5 has a `busca` row mentioning `titulo`, `endereco__bairro` and `search_param`.
    - §7.4 states the five `ordenacao` values were adopted as working values (D-11) pending E2.
    - §7.5 exists with `preco_venda`, "nulls last", `preco_aluguel` for Phase 3, and the CursorPagination non-nullable risk with `Coalesce` listed as an option (not decided).
    - `## 9. Adendos da Fase 2` exists; sections §1-§8 are all still present.
  </acceptance_criteria>
  <done>The frozen contract carries this phase's three addenda plus the /cidades pagination facts, clearly marked as pending E2 sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> GET /api/publico/cidades/ | Anonymous clients (app, site) call a public, unauthenticated endpoint |
| manage.py command -> database | A dev-only command writes Cidade/Empresa rows |
| agent -> sibling git repo | Changes in a repo whose owner requires explicit commit authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | CidadesPublicasAPIView.queryset | medium | mitigate | Served-only rule enforced in the queryset (`empresas_atuantes__ativa=True`, `.distinct()`), never from request params; tests assert cities without an active company never appear |
| T-02-02-02 | Information Disclosure | CidadeSerializer | medium | mitigate | Reused allowlist serializer (`fields = ["id", "nome", "uf"]`); test asserts each row's key set is exactly {id, nome, uf} |
| T-02-02-03 | Elevation of Privilege | CidadesPublicasAPIView | low | mitigate | `AllowAny` only on a read-only `ListAPIView`; test asserts POST -> 405; global IsAuthenticated default untouched for every other view |
| T-02-02-04 | Denial of Service | CidadeCursorPagination | low | mitigate | Fixed `page_size = 50`, no `page_size_query_param`, stable indexed ordering (nome, uf) |
| T-02-02-05 | Tampering | semear_vitrine_dev | medium | mitigate | Raises CommandError unless settings.DEBUG; looks up only its own marker Empresa by name, never modifies other companies; idempotent get_or_create |
| T-02-02-06 | Information Disclosure | .env.example ALLOWED_HOSTS | low | accept | Template-only change adding the emulator alias 10.0.2.2 for development; production sets its own ALLOWED_HOSTS via its real .env |
| T-02-02-07 | Repudiation | sibling repo commits | low | mitigate | No commit/push in ../imoveis-aqui; verify step asserts HEAD == main on feat/APP02; human-check obtains explicit authorization |
</threat_model>

<verification>
- Django `empresas` tests pass on in-memory SQLite; `manage.py check` clean.
- Sibling repo on `feat/APP02` with HEAD == main (no unauthorized commit).
- Contract document contains all addendum markers and still has §1-§8.
</verification>

<success_criteria>
- `GET /api/publico/cidades/` is public, served-only, allowlisted, cursor-paginated and tested (API-02).
- The dev database can be seeded with the same 4 cities the app mock uses (Pitfall 4).
- D-05/D-11/D-12 addenda and the DRF nulls-last risk are recorded for E2.
</success_criteria>

<output>
Create `.planning/phases/02-vitrine-lista-busca-e-ordena-o/02-02-SUMMARY.md` when done
</output>
