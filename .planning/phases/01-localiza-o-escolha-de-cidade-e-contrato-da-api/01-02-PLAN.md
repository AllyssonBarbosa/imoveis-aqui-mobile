---
phase: 01-localiza-o-escolha-de-cidade-e-contrato-da-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md
  - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/cidades.example.json
  - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/imoveis.example.json
autonomous: true
requirements: [API-01]
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Existe um documento de contrato escrito e versionado (01-CONTRATO-API.md) cobrindo GET /cidades e GET /imoveis — nomes de campo, enums, nomes de parâmetro de filtro e envelope de paginação — antes de qualquer código de APP02/APP03 ou do endpoint real (API-01)."
    - "O envelope de paginação documentado é cursor (DRF CursorPagination): resposta {next, previous, results}, sem count total nem pulo de página (D-01)."
    - "Os campos de tipologia que ainda não existem no model (natureza casa/apto/terreno/lote, quartos, suites, vagas, area) estão no contrato marcados explicitamente como PENDENTE E2 (D-02)."
    - "Os nomes de campo e query params são português snake_case (cidade, preco_min, preco_max, quartos_min, natureza, finalidade, ordenacao, cursor) (D-04)."
    - "O contrato especifica que GET /imoveis usa um serializer público com allowlist explícita (sem proprietario/documento/corretor/empresa interno) e filtra publicado=True no queryset, preservando o isolamento multitenant."
    - "A forma de assets/cidades.json (envelope cursor, campos id/nome/uf) é idêntica à documentada para GET /cidades, e a regra de filtragem da lista de cidades atendidas (empresas_atuantes__ativa=True) está escrita no contrato (D-13, D-16, Pitfall 4)."
    - "Os itens genuinamente em aberto — quartos/suites/vagas exact-vs-range, formato do param cidade (nome+uf vs id), preço exibido para VENDA_E_ALUGUEL, valores do enum ordenacao — estão marcados como pendentes de sign-off da frente web, nunca chutados como congelados."
  artifacts:
    - path: ".planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md"
      provides: "frozen written contract for GET /cidades and GET /imoveis"
      contains: "CursorPagination"
    - path: ".planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/cidades.example.json"
      provides: "example GET /cidades response, doubles as the Phase 2/3 mock fixture (API-04)"
      contains: "results"
    - path: ".planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/imoveis.example.json"
      provides: "example GET /imoveis response in the frozen shape, mock fixture for Phase 2/3 (API-04)"
      contains: "results"
  key_links:
    - from: ".planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/cidades.example.json"
      to: "assets/cidades.json"
      via: "identical envelope + field shape so Phase 2's real GET /cidades drops in without changing fromJson (D-13)"
      pattern: "results"
  prohibitions:
    - "the contract must never authorize client-side filtering, sorting, or search — every filter, sort, and search is resolved server-side and the response arrives ready"
    - "the public /imoveis serializer must never expose owner, document, broker, or company-internal fields — only the explicit allowlist"
---

<objective>
Audit the real Django models in the sibling API repo and freeze, in writing, the contract for
`GET /cidades` and `GET /imoveis` — field names, enums, filter param names, and the cursor
pagination envelope — plus example JSON responses that double as the Phase 2/3 mock fixtures.

Purpose: this is the coordination point with the web team (E2). APP02/APP03 and the real
`/imoveis` endpoint all build against this frozen shape; freezing it now, grounded in the real
models and with the genuinely-open items explicitly flagged, prevents silent drift (API-01).
Output: `01-CONTRATO-API.md` + `contrato/cidades.example.json` + `contrato/imoveis.example.json`.
This half is independent of the app code — it runs in parallel with Plan 01-01.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTEXT.md
@.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit sibling models and write the frozen API contract doc</name>
  <precondition>The sibling API repo is readable at ../imoveis-aqui/Web/localizacao/models.py and ../imoveis-aqui/Web/imoveis/models.py (source-of-truth for the real field shapes)</precondition>
  <files>.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md</files>
  <read_first>
    - ../imoveis-aqui/Web/localizacao/models.py (Cidade: nome, uf, unique(nome, uf))
    - ../imoveis-aqui/Web/imoveis/models.py (Imovel: finalidade, preco_venda, preco_aluguel, descricao, caracteristicas, publicado — confirm which tipologia fields do NOT exist)
    - ../imoveis-aqui/Web/core/models.py (Endereco: bairro, cidade FK, latitude, longitude; EmpresaOwnedModel multitenant base)
    - ../imoveis-aqui/Web/empresas/models.py (Empresa.cidades_atuacao M2M, related_name empresas_atuantes, ativa flag)
    - ../imoveis-aqui/Web/empresas/api/serializers.py (existing CidadeSerializer fields [id, nome, uf]) and ../imoveis-aqui/Web/empresas/api/views.py (EmpresaPublicaAPIView AllowAny + filter(ativa=True) precedent)
    - ../imoveis-aqui/Web/imoveis/api/serializers.py, views.py (authenticated ImovelViewSet — the fields the public serializer must NOT reuse)
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-13, D-16)
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-RESEARCH.md (§Code Examples fixture shapes; §Common Pitfalls #4; §Open Questions 2/3/4; Assumptions A4/A5/A6/A7/A8; §Security Domain)
  </read_first>
  <action>
    Write `01-CONTRATO-API.md` (Markdown, human-legible per D-03). Structure it with these sections,
    grounded in the model files read above (never invented):

    1. Audit summary — state the corrected reality (the Imovel model EXISTS with finalidade, preços,
       descrição, características, endereço, fotos, publicado; what is genuinely missing is tipologia
       and the public endpoints), superseding PROJECT.md's stale "model doesn't exist" note.
    2. GET /cidades — a field table (`id` int, `nome` string, `uf` 2-char) matching the existing
       `CidadeSerializer`; the cursor envelope `{next, previous, results}` (D-01); and the served-set
       rule: the list is `Cidade.objects.filter(empresas_atuantes__ativa=True).distinct()`, NOT
       `Cidade.objects.all()` (Pitfall 4 / A3) — write this rule explicitly so Phase 2's real
       endpoint applies it identically to the fixture.
    3. GET /imoveis — a field table grounded in the real Imovel + Endereco + Cidade fields
       (titulo, finalidade enum, preco_venda, preco_aluguel, descricao, bairro from Endereco,
       cidade nested {id,nome,uf}, foto_capa, caracteristicas, criado_em). Add the tipologia fields
       (natureza with enum CASA/APARTAMENTO/TERRENO/LOTE, quartos, suites, vagas, area) in the same
       table but clearly tagged **PENDENTE E2** — they do not exist on the model yet (D-02).
    4. Pagination — document DRF `CursorPagination`: `{next, previous, results}`, ordered by a stable
       field (e.g. `-criado_em`), the `cursor` query param, no `count`/no page jump; state the D-01
       rationale (immunizes infinite scroll against duplicated/shuffled cards) and mark it one-way.
    5. Query params for GET /imoveis — português snake_case (D-04): `cidade`, `preco_min`,
       `preco_max`, `quartos_min`, `suites_min`, `vagas_min`, `natureza`, `finalidade`, `bairro`,
       `area_min`, `area_max`, `caracteristicas`, `ordenacao`, `cursor`.
    6. Public serializer / security — an explicit allowlist section: the public endpoint uses a
       DEDICATED public serializer, never the authenticated `ImovelSerializer`; it must NOT expose
       proprietário, documento, corretor_responsavel, or empresa-internal fields; it filters
       `publicado=True` at the queryset level and preserves multitenant isolation (mirroring the
       `EmpresaPublicaAPIView` / `EmpresaScopedQuerySetMixin` precedent). This is a high-weight
       evaluation criterion — write it as a hard requirement for Phase 4.
    7. **Itens PENDENTES de sign-off da frente web** — a dedicated section listing, unresolved and
       NOT frozen: quartos/suites/vagas exact-match vs range (A5, present both param namings side by
       side and require an explicit choice); `cidade` param format nome+uf vs numeric id (A8, recommend
       nome+uf for consistency with D-15 but require agreement); which price the card shows for
       `VENDA_E_ALUGUEL` (A6, Phase-2 UI decision, flag it); `ordenacao` enum values
       (preco_asc/preco_desc/area_asc/area_desc/mais_recentes — A7, naming guess, easy to change).
    8. Sync note — per D-03 the doc is versioned in BOTH repos; note that copying it into
       `../imoveis-aqui/Web` and obtaining E2 sign-off is the manual coordination step (human-check),
       and the contract is only "congelado" after that sign-off.

    Do NOT guess a single answer for any PENDENTE item; presenting the options and requiring
    web-team sign-off is the deliverable, not a decision to make here.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md</automated>
    <fails_when>non-zero exit (contract file absent)</fails_when>
    <automated>CDOC=.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md; for TOKEN in "GET /cidades" "GET /imoveis" "CursorPagination" "PENDENTE E2" "publicado" "allowlist"; do grep -Fq "$TOKEN" "$CDOC" || { echo "MISSING: $TOKEN"; exit 1; }; done</automated>
    <fails_when>non-zero exit, or any "MISSING: &lt;token&gt;" line (a required section token is absent)</fails_when>
  </verify>
  <acceptance_criteria>
    - `01-CONTRATO-API.md` documents both endpoints with field tables grounded in the real sibling models (not invented shapes).
    - The cursor envelope `{next, previous, results}` is specified for both endpoints (D-01).
    - Tipologia fields are present but tagged PENDENTE E2 (D-02); the genuinely-open items (quartos/suites/vagas semantics, cidade param format, VENDA_E_ALUGUEL price, ordenacao enum) are in a dedicated "pendente de sign-off" section, not frozen.
    - The public-serializer allowlist + `publicado=True` + multitenant-isolation requirement is written explicitly.
    - Query params are português snake_case (D-04).
  </acceptance_criteria>
  <done>The frozen contract doc exists, is grounded in the audited models, marks PENDENTE E2 and open items honestly, and specifies the public-serializer allowlist discipline.</done>
</task>

<task type="auto">
  <name>Task 2: Write the example JSON fixtures (mock fixtures for Phase 2/3)</name>
  <reversibility rating="one-way">Cursor pagination + the frozen response shape (D-01) is one-way: changing the pagination scheme or field shape after the web team agrees breaks the contract and the parsing of every paginated response in the app. Not gated — the human already chose cursor in CONTEXT.md D-01 (gating a decision already made is an anti-pattern); the rating is recorded for later phases.</reversibility>
  <files>.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/cidades.example.json, .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/imoveis.example.json</files>
  <read_first>
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTRATO-API.md (the shapes just written in Task 1 — source of truth)
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-RESEARCH.md (§Code Examples — the cursor-paginated GET /imoveis fixture and the assets/cidades.json fixture shapes)
    - .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-CONTEXT.md (D-03 — JSON examples double as mock fixtures for API-04)
  </read_first>
  <action>
    Create `contrato/cidades.example.json` and `contrato/imoveis.example.json` under the phase
    directory. Each mirrors exactly the shape documented in Task 1 (D-03): the cursor envelope
    `{next, previous, results:[...]}`. `cidades.example.json` uses `{id, nome, uf}` rows and must be
    the identical shape the app's `assets/cidades.json` uses (so Phase 2's real endpoint drops in
    without changing `fromJson`). `imoveis.example.json` includes the real fields plus the tipologia
    fields with representative values, and a top-of-file comment-free JSON (pure JSON) — but note in
    the contract doc which fields are PENDENTE E2. Use realistic served-city rows consistent with
    `assets/cidades.json` (e.g. Campinas/SP). These files are the fixtures the Phase 2/3 mock
    DataSource will read (API-04).
  </action>
  <verify>
    <automated>python3 -m json.tool .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/cidades.example.json >/dev/null && python3 -m json.tool .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/imoveis.example.json >/dev/null</automated>
    <fails_when>non-zero exit (either file absent or not valid JSON)</fails_when>
    <automated>grep -Fq '"results"' .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/cidades.example.json && grep -Fq '"results"' .planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/contrato/imoveis.example.json</automated>
    <fails_when>non-zero exit (the cursor envelope "results" key is absent from either fixture)</fails_when>
  </verify>
  <acceptance_criteria>
    - Both fixtures are valid JSON in the cursor envelope `{next, previous, results:[...]}`.
    - `cidades.example.json` is the identical shape as the app's `assets/cidades.json` (D-13).
    - `imoveis.example.json` carries the documented fields including the PENDENTE-E2 tipologia fields, ready to serve as the Phase 2/3 mock fixture (API-04).
  </acceptance_criteria>
  <done>Both example JSON fixtures exist, parse as valid JSON, carry the cursor envelope, and match the frozen contract shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future public endpoint → any client | `GET /imoveis` / `GET /cidades` will be `AllowAny` (no token). The contract must constrain what a public, unauthenticated caller can see and how the queryset is scoped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | future public `/imoveis` serializer (Phase 4) | high | mitigate | Contract mandates a DEDICATED public serializer with an explicit field allowlist — never reuse the authenticated `ImovelSerializer`; no proprietário/documento/corretor/empresa-internal fields. Documented now so Phase 4 does not rediscover it. |
| T-01-02-02 | Elevation of Privilege / Tampering | multitenant isolation via `cidade`/`id` param manipulation on future public endpoint | high | mitigate | Contract mandates `publicado=True` filtering at the queryset level (not per-request trust), mirroring `EmpresaScopedQuerySetMixin` — no imóvel from another empresa or unpublished leaks. |
| T-01-02-03 | Information Disclosure | contract doc itself references sibling-repo internals | low | accept | The doc names field allowlists and model names, not secrets/credentials; it is a design artifact meant to be shared with the web team (D-03). |
</threat_model>

<artifacts_produced>
Symbols/files this plan creates:
- `01-CONTRATO-API.md` — frozen written contract for `GET /cidades` and `GET /imoveis`: field tables, `finalidade`/`natureza` enums, PT snake_case query params (`cidade`, `preco_min`, `preco_max`, `quartos_min`, `suites_min`, `vagas_min`, `natureza`, `finalidade`, `bairro`, `area_min`, `area_max`, `caracteristicas`, `ordenacao`, `cursor`), cursor pagination envelope, public-serializer allowlist rule, served-set filter rule, and the PENDENTE-E2 / pending-sign-off sections.
- `contrato/cidades.example.json` — example GET /cidades response (doubles as Phase 2/3 mock fixture).
- `contrato/imoveis.example.json` — example GET /imoveis response in the frozen shape (Phase 2/3 mock fixture, API-04).
</artifacts_produced>

<verification>
- Contract doc exists and contains all required section tokens (automated grep gate in Task 1).
- Both example fixtures are valid JSON with the cursor envelope (automated gate in Task 2).
- Web-team (E2) sign-off + cross-repo placement into `../imoveis-aqui/Web` is a manual coordination step, harvested at end-of-phase (see human-check below and VALIDATION.md Manual-Only Verifications).
</verification>

<success_criteria>
The written contract for `GET /cidades` and `GET /imoveis` is frozen (field names, enums, filter
params, cursor pagination envelope), grounded in the real sibling models, with tipologia and the
genuinely-open items honestly flagged for web-team sign-off — ready to orient APP02/APP03 and the
Phase 4 endpoint. API-01 satisfied (pending the manual E2 sign-off).

**Human verification (harvested end-of-phase):** review `01-CONTRATO-API.md`; confirm GET /cidades
and GET /imoveis shapes are present, the cursor envelope and allowlist rule are specified, and the
open items are explicitly flagged for web-team sign-off; copy the doc into `../imoveis-aqui/Web`
and obtain E2 agreement before the contract is treated as truly "congelado".
</success_criteria>

<output>
Create `.planning/phases/01-localiza-o-escolha-de-cidade-e-contrato-da-api/01-02-SUMMARY.md` when done.
</output>