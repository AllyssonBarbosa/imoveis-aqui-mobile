---
id: 260922-jk3
slug: reescrever-o-readme-md-do-projeto-hoje-b
type: quick
status: planned
created: 2026-09-22
---

<objective>
Substituir o README.md padrão do `flutter create` por um README em português que explique como preparar o ambiente e executar o app "Imóveis Aqui — App (Vitrine)". Documentação apenas — não altera código-fonte, comportamento ou dependências. Não inventar comandos que não existam no projeto.
</objective>

<tasks>

## Tarefa 1 — Reescrever README.md

**files:** `README.md`

**action:** Substituir todo o conteúdo boilerplate por um README em português com estas seções, nesta ordem:

1. **Título e descrição curta** — "Imóveis Aqui — App (Vitrine)": app Flutter de um marketplace de imóveis por cidade; abre direto na vitrine da cidade do visitante, sem login. Mencionar que esta fatia cobre APP01–APP03 (abertura/localização/escolha da cidade, vitrine com lista/busca/ordenação, filtros da vitrine) e que nenhuma regra de negócio é decidida no app — tudo vem calculado/filtrado da API Django.

2. **Pré-requisitos** —
   - Flutter 3.47.5 stable / Dart 3.13.4 (canal stable). No macOS (Apple Silicon/M1): instalar pelo instalador oficial (https://docs.flutter.dev/get-started/install) ou via Homebrew: `brew install --cask flutter`. Confirmar com `flutter --version`.
   - Um dispositivo/emulador Android **ou** simulador iOS (os alvos de deploy são Android e iOS). Rodar `flutter doctor` para checar o toolchain.

3. **Preparar o ambiente (setup)** —
   - `flutter pub get`
   - `dart run build_runner build --delete-conflicting-outputs` (codegen de freezed / json_serializable / injectable — gera os arquivos `*.freezed.dart`, `*.g.dart`, `injection.config.dart`)

4. **Rodar o app** —
   - `flutter run` (com um device/emulador ativo)

5. **Testes e análise** —
   - `flutter test` (a suíte atual tem 53 testes)
   - `flutter analyze`

6. **Permissão de localização** —
   - Android declara `ACCESS_FINE_LOCATION` e `ACCESS_COARSE_LOCATION`; iOS declara `NSLocationWhenInUseUsageDescription`.
   - O app só pede a permissão a partir do toque no CTA da tela de priming (nunca na inicialização) e, se a localização for recusada/indisponível, cai no fallback de escolha manual da cidade — nunca numa tela de erro.

7. **Arquitetura (nota curta)** —
   - Clean Architecture em camadas `lib/data/` (models, datasources, repositories), `lib/domain/` (entities, use cases, interfaces) e `lib/presentation/` (widgets + Cubits). Estado com Cubit (flutter_bloc), estados imutáveis (freezed sealed), erros via `Result` selado. DI com get_it/injectable. Idioma do código em português.

**verify:** README.md não contém mais o texto "A new Flutter project"; contém os comandos `flutter pub get`, `dart run build_runner build --delete-conflicting-outputs`, `flutter run`, `flutter test`, `flutter analyze` e `brew install --cask flutter`; está em português.

**done:** README.md reescrito e commitado.

</tasks>
